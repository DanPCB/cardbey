# Cardbey Android

Native Kotlin / Jetpack Compose client. See [docs/android/README.md](../../docs/android/README.md).

```bash
./gradlew :app:assembleDevDebug
```

Open `apps/android` in Android Studio.
