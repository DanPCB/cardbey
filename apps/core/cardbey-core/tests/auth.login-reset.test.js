/**
 * Placeholder: empty file caused "No test suite found" and broke `npm test`.
 */
import { describe, it } from 'vitest';

describe('auth.login-reset', () => {
  it.todo('add login / password-reset route coverage');
});
