/**
 * Copy/rewrite agent (stub). Real implementation in later phase.
 * Route calls: runCopyAgent(agentParams, agentOpts).
 */

export async function runCopyAgent(context, plan) {
  return { status: 'completed', artifacts: [] };
}
