/**
 * Catalog agent (stub). Real implementation in later phase.
 * Route calls: runCatalogAgent(agentParams, agentOpts).
 */

export async function runCatalogAgent(context, plan) {
  return { status: 'completed', artifacts: [] };
}
