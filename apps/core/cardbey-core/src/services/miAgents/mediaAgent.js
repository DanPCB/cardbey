/**
 * Media agent (stub). Real implementation in later phase.
 * Route calls: runMediaAgent(context, { missionContext, emitContextUpdate }).
 */

export async function runMediaAgent(context, plan) {
  return { status: 'completed', artifacts: [] };
}
