--- a/apps/dashboard/cardbey-marketing-dashboard/src/App.jsx
+++ b/apps/dashboard/cardbey-marketing-dashboard/src/App.jsx
-import ConsoleHomeWorkspace from "./app/console/ConsoleHomeWorkspace";
+import DevelopmentCenterPage from './pages/development/DevelopmentCenterPage.jsx';
-import MissionLogsListView from "./app/console/missions/MissionLogsListView";
+import ConsoleHomeWorkspace from "./app/console/ConsoleHomeWorkspace";
-import MissionDetailView from "./app/console/missions/MissionDetailView";
+import MissionLogsListView from "./app/console/missions/MissionLogsListView";
-import MissionArtifactCreativeShell from "./app/console/missions/MissionArtifactCreativeShell";
+import MissionDetailView from "./app/console/missions/MissionDetailView";
-import ConsoleSectionPlaceholder from "./app/console/ConsoleSectionPlaceholder";
+import MissionArtifactCreativeShell from "./app/console/missions/MissionArtifactCreativeShell";
-import MissionConsoleTelemetryPage from "./app/console/telemetry/MissionConsoleTelemetryPage";
+import ConsoleSectionPlaceholder from "./app/console/ConsoleSectionPlaceholder";
-import InsightsSectionPage from "./app/console/sections/InsightsSectionPage";
+import MissionConsoleTelemetryPage from "./app/console/telemetry/MissionConsoleTelemetryPage";
-import DevicesSectionPage from "./app/console/sections/DevicesSectionPage";
+import InsightsSectionPage from "./app/console/sections/InsightsSectionPage";
-import CreativesSectionPage from "./app/console/sections/CreativesSectionPage";
+import DevicesSectionPage from "./app/console/sections/DevicesSectionPage";
-import ControlTowerPage from "./app/console/control-tower/ControlTowerPage";
+import CreativesSectionPage from "./app/console/sections/CreativesSectionPage";
-
+import ControlTowerPage from "./app/console/control-tower/ControlTowerPage";
-// Placeholder components for simple pages
+
-function CatalogPlaceholder() {
+// Placeholder components for simple pages
-  const { t } = useTranslation('dashboard');
+function CatalogPlaceholder() {
-  return (
+  const { t } = useTranslation('dashboard');
-    <div className="flex h-screen items-center justify-center bg-background">
+  return (
-      <div className="text-center">
+    <div className="flex h-screen items-center justify-center bg-background">
-        <h1 className="mb-4 text-4xl font-bold text-foreground">{t('catalog.title')}</h1>
+      <div className="text-center">
-        <p className="text-muted-foreground">{t('catalog.comingSoon')}</p>
+        <h1 className="mb-4 text-4xl font-bold text-foreground">{t('catalog.title')}</h1>
-      </div>
+        <p className="text-muted-foreground">{t('catalog.comingSoon')}</p>
-    </div>
+      </div>
-  );
+    </div>
-}
+  );
-
+}
-function OrdersPlaceholder() {
+
-  const { t } = useTranslation('dashboard');
+function OrdersPlaceholder() {
-  return (
+  const { t } = useTranslation('dashboard');
-    <div className="flex h-screen items-center justify-center bg-background">
+  return (
-      <div className="text-center">
+    <div className="flex h-screen items-center justify-center bg-background">
-        <h1 className="mb-4 text-4xl font-bold text-foreground">{t('orders.title')}</h1>
+      <div className="text-center">
-        <p className="text-muted-foreground">{t('orders.comingSoon')}</p>
+        <h1 className="mb-4 text-4xl font-bold text-foreground">{t('orders.title')}</h1>
-        <div className="mt-4 flex items-center justify-center gap-2">
+        <p className="text-muted-foreground">{t('orders.comingSoon')}</p>
-          <a
+        <div className="mt-4 flex items-center justify-center gap-2">
-            href="/orders/seller"
+          <a
-            className="inline-flex items-center px-4 py-2 rounded-lg bg-primary text-primary-foreground hover:bg-primary/90 transition"
+            href="/orders/seller"
-          >
+            className="inline-flex items-center px-4 py-2 rounded-lg bg-primary text-primary-foreground hover:bg-primary/90 transition"
-            Open Seller Orders (Validation UI)
+          >
-          </a>
+            Open Seller Orders (Validation UI)
-        </div>
+          </a>
-      </div>
+        </div>
-    </div>
+      </div>
-  );
+    </div>
-}
+  );
-
+}
-// Create a client for React Query
+
-const queryClient = new QueryClient({
+// Create a client for React Query
-  defaultOptions: {
+const queryClient = new QueryClient({
-    queries: {
+  defaultOptions: {
-      refetchOnWindowFocus: false,
+    queries: {
-      retry: 1,
+      refetchOnWindowFocus: false,
-      staleTime: 30_000, // 30 seconds
+      retry: 1,
-    },
+      staleTime: 30_000, // 30 seconds
-  },
+    },
-});
+  },
-
+});
-function readSession() {
+
-  const t = getStorageTokens();
+function readSession() {
-  // Role comes from stored user.role (set after login), not from token presence
+  const t = getStorageTokens();
-  const role = (t.role && t.role.trim()) ? t.role : null;
+  // Role comes from stored user.role (set after login), not from token presence
-  const user = t.username || "";
+  const role = (t.role && t.role.trim()) ? t.role : null;
-  return { role, user };
+  const user = t.username || "";
-}
+  return { role, user };
-function useSession() {
+}
-  const [sess, setSess] = useState(readSession());
+function useSession() {
-  useEffect(() => {
+  const [sess, setSess] = useState(readSession());
-    const update = () => setSess(readSession());
+  useEffect(() => {
-    window.addEventListener("storage", update);
+    const update = () => setSess(readSession());
-    window.addEventListener("authchange", update);
+    window.addEventListener("storage", update);
-    return () => {
+    window.addEventListener("authchange", update);
-      window.removeEventListener("storage", update);
+    return () => {
-      window.removeEventListener("authchange", update);
+      window.removeEventListener("storage", update);
-    };
+      window.removeEventListener("authchange", update);
-  }, []);
+    };
-  return sess;
+  }, []);
-}
+  return sess;
-
+}
-function RequireAuth({ children }) {
+
-  const tokens = getStorageTokens();
+function RequireAuth({ children }) {
-  const { user, isLoading } = useCurrentUser();
+  const tokens = getStorageTokens();
-  const navigate = useNavigate();
+  const { user, isLoading } = useCurrentUser();
-  const location = useLocation();
+  const navigate = useNavigate();
-  const hasNavigatedRef = useRef(false);
+  const location = useLocation();
-  const realAuthed = isRealAuthed(tokens, user, isLoading);
+  const hasNavigatedRef = useRef(false);
-  const path = location.pathname || (typeof window !== 'undefined' ? window.location.pathname : '') || '';
+  const realAuthed = isRealAuthed(tokens, user, isLoading);
-  const isConsoleEntry =
+  const path = location.pathname || (typeof window !== 'undefined' ? window.location.pathname : '') || '';
-    path === '/app' ||
+  const isConsoleEntry =
-    path === '/app/' ||
+    path === '/app' ||
-    path.startsWith('/app/missions') ||
+    path === '/app/' ||
-    path.startsWith('/app/store/temp/') ||
+    path.startsWith('/app/missions') ||
-    path.startsWith('/app/store/draft/') ||
+    path.startsWith('/app/store/temp/') ||
-    path.startsWith('/app/console') ||
+    path.startsWith('/app/store/draft/') ||
-    path === '/console' ||
+    path.startsWith('/app/console') ||
-    path === '/console/' ||
+    path.startsWith('/app/development') ||
-    path.startsWith('/console/missions') ||
+    path === '/console' ||
-    path.startsWith('/console/store/temp/') ||
+    path === '/console/' ||
-    path.startsWith('/console/store/draft/') ||
+    path.startsWith('/console/missions') ||
-    path.startsWith('/console/control-tower');
+    path.startsWith('/console/store/temp/') ||
-
+    path.startsWith('/console/store/draft/') ||
-  useEffect(() => {
+    path.startsWith('/console/control-tower');
-    if (isConsoleEntry) return;
+
-    if (!realAuthed && !isLoading && !hasNavigatedRef.current) {
+  useEffect(() => {
-      const returnTo = encodeURIComponent(location.pathname + location.search);
+    if (isConsoleEntry) return;
-      const target = `/login?returnTo=${returnTo}`;
+    if (!realAuthed && !isLoading && !hasNavigatedRef.current) {
-      if (location.pathname !== '/login') {
+      const returnTo = encodeURIComponent(location.pathname + location.search);
-        hasNavigatedRef.current = true;
+      const target = `/login?returnTo=${returnTo}`;
-        setTimeout(() => navigate(target, { replace: true }), 0);
+      if (location.pathname !== '/login') {
-      }
+        hasNavigatedRef.current = true;
-    }
+        setTimeout(() => navigate(target, { replace: true }), 0);
-  }, [realAuthed, isLoading, isConsoleEntry, location.pathname, location.search, navigate]);
+      }
-
+    }
-  useEffect(() => {
+  }, [realAuthed, isLoading, isConsoleEntry, location.pathname, location.search, navigate]);
-    hasNavigatedRef.current = false;
+
-  }, [location.pathname]);
+  useEffect(() => {
-
+    hasNavigatedRef.current = false;
-  if (isConsoleEntry) return children;
+  }, [location.pathname]);
-  if (isLoading || !realAuthed) {
+
-    return null;
+  if (isConsoleEntry) return children;
-  }
+  if (isLoading || !realAuthed) {
-  return children;
+    return null;
-}
+  }
-
+  return children;
-/** Safe same-origin path from returnTo param (no protocol/host). */
+}
-function getSafeReturnTo(searchParams) {
+
-  const raw = searchParams.get("returnTo");
+/** Safe same-origin path from returnTo param (no protocol/host). */
-  if (!raw) return null;
+function getSafeReturnTo(searchParams) {
-  try {
+  const raw = searchParams.get("returnTo");
-    const path = decodeURIComponent(raw);
+  if (!raw) return null;
-    if (typeof path !== "string" || !path.startsWith("/") || path.startsWith("//")) return null;
+  try {
-    return path;
+    const path = decodeURIComponent(raw);
-  } catch {
+    if (typeof path !== "string" || !path.startsWith("/") || path.startsWith("//")) return null;
-    return null;
+    return path;
-  }
+  } catch {
-}
+    return null;
-
+  }
-function RedirectIfAuthed({ children }) {
+}
-  const tokens = getStorageTokens();
+
-  const { user, isLoading } = useCurrentUser();
+function RedirectIfAuthed({ children }) {
-  const [searchParams] = useSearchParams();
+  const tokens = getStorageTokens();
-  const realAuthed = isRealAuthed(tokens, user, isLoading);
+  const { user, isLoading } = useCurrentUser();
-  if (!realAuthed) return children;
+  const [searchParams] = useSearchParams();
-  const returnTo = getSafeReturnTo(searchParams);
+  const realAuthed = isRealAuthed(tokens, user, isLoading);
-  return <Navigate to={returnTo || authenticatedHome()} replace />;
+  if (!realAuthed) return children;
-}
+  const returnTo = getSafeReturnTo(searchParams);
-
+  return <Navigate to={returnTo || authenticatedHome()} replace />;
-/** Platform admin only: require auth then role === 'admin', else redirect to /dashboard (no client-side privilege escalation). */
+}
-function AdminRoute({ children }) {
+
-  return (
+/** Platform admin only: require auth then role === 'admin', else redirect to /dashboard (no client-side privilege escalation). */
-    <RequireAuth>
+function AdminRoute({ children }) {
-      <AdminRouteInner>{children}</AdminRouteInner>
+  return (
-    </RequireAuth>
+    <RequireAuth>
-  );
+      <AdminRouteInner>{children}</AdminRouteInner>
-}
+    </RequireAuth>
-function AdminRouteInner({ children }) {
+  );
-  const { user, isLoading } = useCurrentUser();
+}
-  if (isLoading) return null;
+function AdminRouteInner({ children }) {
-  if (user?.role !== 'admin') return <Navigate to="/dashboard" replace />;
+  const { user, isLoading } = useCurrentUser();
-  return children;
+  if (isLoading) return null;
-}
+  if (user?.role !== 'admin') return <Navigate to="/dashboard" replace />;
-
+  return children;
-/** Super admin only (matches API requireSuperAdmin). */
+}
-function SuperAdminRoute({ children }) {
+
-  return (
+/** Super admin only (matches API requireSuperAdmin). */
-    <RequireAuth>
+function SuperAdminRoute({ children }) {
-      <SuperAdminRouteInner>{children}</SuperAdminRouteInner>
+  return (
-    </RequireAuth>
+    <RequireAuth>
-  );
+      <SuperAdminRouteInner>{children}</SuperAdminRouteInner>
-}
+    </RequireAuth>
-function SuperAdminRouteInner({ children }) {
+  );
-  const { user, isLoading } = useCurrentUser();
+}
-  if (isLoading) return null;
+function SuperAdminRouteInner({ children }) {
-  if (user?.role !== 'super_admin') return <Navigate to="/dashboard" replace />;
+  const { user, isLoading } = useCurrentUser();
-  return children;
+  if (isLoading) return null;
-}
+  if (user?.role !== 'super_admin') return <Navigate to="/dashboard" replace />;
-
+  return children;
-/** Platform admin (platform_admin, super_admin, legacy admin) — matches ingestion metrics API. */
+}
-function PlatformAdminRoute({ children }) {
+
-  return (
+/** Platform admin (platform_admin, super_admin, legacy admin) — matches ingestion metrics API. */
-    <RequireAuth>
+function PlatformAdminRoute({ children }) {
-      <PlatformAdminRouteInner>{children}</PlatformAdminRouteInner>
+  return (
-    </RequireAuth>
+    <RequireAuth>
-  );
+      <PlatformAdminRouteInner>{children}</PlatformAdminRouteInner>
-}
+    </RequireAuth>
-function PlatformAdminRouteInner({ children }) {
+  );
-  const { user, isLoading } = useCurrentUser();
+}
-  if (isLoading) {
+function PlatformAdminRouteInner({ children }) {
-    return (
+  const { user, isLoading } = useCurrentUser();
-      <div className="flex min-h-[40vh] items-center justify-center p-8 text-sm text-muted-foreground">
+  if (isLoading) {
-        Loading…
+    return (
-      </div>
+      <div className="flex min-h-[40vh] items-center justify-center p-8 text-sm text-muted-foreground">
-    );
+        Loading…
-  }
+      </div>
-  if (!resolvePlatformAdmin(user)) return <Navigate to="/dashboard" replace />;
+    );
-  return children;
+  }
-}
+  if (!resolvePlatformAdmin(user)) return <Navigate to="/dashboard" replace />;
-
+  return children;
-/** Store review: for pre-publish lineage (`temp` or `draft`) allow guest to land and view (read-only; Gate 1 on actions). For real store IDs require auth. */
+}
-function StoreReviewGate({ children }) {
+
-  const { storeId } = useParams();
+/** Store review: for pre-publish lineage (`temp` or `draft`) allow guest to land and view (read-only; Gate 1 on actions). For real store IDs require auth. */
-  if (isDraftLineagePathSegment(storeId)) return <>{children}</>;
+function StoreReviewGate({ children }) {
-  return <RequireAuth>{children}</RequireAuth>;
+  const { storeId } = useParams();
-}
+  if (isDraftLineagePathSegment(storeId)) return <>{children}</>;
-
+  return <RequireAuth>{children}</RequireAuth>;
-/**
+}
- * Allow access for real auth OR guest session. Used for /app (Mission Control) so guests can
+
- * create missions, generate drafts, and review without signing in. Auth is required at publish/claim.
+/**
- * If user has no token, create a guest session once (same as Quick Start) then allow access.
+ * Allow access for real auth OR guest session. Used for /app (Mission Control) so guests can
- * Mission Console entry (/app, /app/, /app/missions, /app/store/temp/*): allow through even if guest creation fails or token is invalid (401)
+ * create missions, generate drafts, and review without signing in. Auth is required at publish/claim.
- * so logged-out users can open the console without Gate 1; Gate 1 triggers on further action when required.
+ * If user has no token, create a guest session once (same as Quick Start) then allow access.
- */
+ * Mission Console entry (/app, /app/, /app/missions, /app/store/temp/*): allow through even if guest creation fails or token is invalid (401)
-/**
+ * so logged-out users can open the console without Gate 1; Gate 1 triggers on further action when required.
- * Guest-allowed console paths — aligned with RequireAuthOrGuest isConsoleEntry.
+ */
- * Guests may create store missions and review drafts without signing in.
+/**
- */
+ * Guest-allowed console paths — aligned with RequireAuthOrGuest isConsoleEntry.
-function isGuestAllowedConsolePath(pathname) {
+ * Guests may create store missions and review drafts without signing in.
-  const path = pathname || '';
+ */
-  if (path.startsWith('/preview/')) return true;
+function isGuestAllowedConsolePath(pathname) {
-  if (path.startsWith('/app/store/temp/') || path.startsWith('/app/store/draft/')) return true;
+  const path = pathname || '';
-  if (path.startsWith('/console/store/temp/') || path.startsWith('/console/store/draft/')) return true;
+  if (path.startsWith('/preview/')) return true;
-  if (path === '/login' || path.startsWith('/signup')) return true;
+  if (path.startsWith('/app/store/temp/') || path.startsWith('/app/store/draft/')) return true;
-  if (
+  if (path.startsWith('/console/store/temp/') || path.startsWith('/console/store/draft/')) return true;
-    path === '/app' ||
+  if (path === '/login' || path.startsWith('/signup')) return true;
-    path === '/app/' ||
+  if (
-    path.startsWith('/app/missions') ||
+    path === '/app' ||
-    path.startsWith('/app/console') ||
+    path === '/app/' ||
-    path === '/console' ||
+    path.startsWith('/app/missions') ||
-    path === '/console/' ||
+    path.startsWith('/app/console') ||
-    path.startsWith('/console/missions') ||
+    path === '/console' ||
-    path.startsWith('/console/control-tower')
+    path === '/console/' ||
-  ) {
+    path.startsWith('/console/missions') ||
-    return true;
+    path.startsWith('/console/control-tower')
-  }
+  ) {
-  return false;
+    return true;
-}
+  }
-
+  return false;
-/**
+}
- * Guests may only use Performer store creation (/app?entry=performer) plus read-only draft preview routes.
+
- * Does not interrupt an in-flight mission on /app — only blocks direct navigation to other areas.
+/**
- */
+ * Guests may only use Performer store creation (/app?entry=performer) plus read-only draft preview routes.
-function GuestPerformerEntryGuard({ children }) {
+ * Does not interrupt an in-flight mission on /app — only blocks direct navigation to other areas.
-  const tokens = getStorageTokens();
+ */
-  const { user, isLoading } = useCurrentUser();
+function GuestPerformerEntryGuard({ children }) {
-  const location = useLocation();
+  const tokens = getStorageTokens();
-  const realAuthed = isRealAuthed(tokens, user, isLoading);
+  const { user, isLoading } = useCurrentUser();
-
+  const location = useLocation();
-  if (realAuthed || isLoading) return children;
+  const realAuthed = isRealAuthed(tokens, user, isLoading);
-  const path = location.pathname || '';
+  if (realAuthed || isLoading) return children;
-  if (isGuestAllowedConsolePath(path)) return children;
+  const path = location.pathname || '';
-  const returnTo = encodeURIComponent(`${path}${location.search || ''}${location.hash || ''}`);
+  if (isGuestAllowedConsolePath(path)) return children;
-  return <Navigate to={`/login?reason=guest_access&returnTo=${returnTo}`} replace />;
+
-}
+  const returnTo = encodeURIComponent(`${path}${location.search || ''}${location.hash || ''}`);
-
+  return <Navigate to={`/login?reason=guest_access&returnTo=${returnTo}`} replace />;
-function RequireAuthOrGuest({ children }) {
+}
-  const tokens = getStorageTokens();
+
-  const { user, isLoading } = useCurrentUser();
+function RequireAuthOrGuest({ children }) {
-  const navigate = useNavigate();
+  const tokens = getStorageTokens();
-  const location = useLocation();
+  const { user, isLoading } = useCurrentUser();
-  const [creatingGuest, setCreatingGuest] = useState(false);
+  const navigate = useNavigate();
-  const createdRef = useRef(false);
+  const location = useLocation();
-  const hasToken = !!(tokens?.bearer || tokens?.adminToken);
+  const [creatingGuest, setCreatingGuest] = useState(false);
-  const allowed = isAuthedOrGuest(tokens, user, isLoading);
+  const createdRef = useRef(false);
-  const path = location.pathname || (typeof window !== 'undefined' ? window.location.pathname : '') || '';
+  const hasToken = !!(tokens?.bearer || tokens?.adminToken);
-  const isConsoleEntry =
+  const allowed = isAuthedOrGuest(tokens, user, isLoading);
-    path === '/app' ||
+  const path = location.pathname || (typeof window !== 'undefined' ? window.location.pathname : '') || '';
-    path === '/app/' ||
+  const isConsoleEntry =
-    path.startsWith('/app/missions') ||
+    path === '/app' ||
-    path.startsWith('/app/store/temp/') ||
+    path === '/app/' ||
-    path.startsWith('/app/store/draft/') ||
+    path.startsWith('/app/missions') ||
-    path.startsWith('/app/console') ||
+    path.startsWith('/app/store/temp/') ||
-    path === '/console' ||
+    path.startsWith('/app/store/draft/') ||
-    path === '/console/' ||
+    path.startsWith('/app/console') ||
-    path.startsWith('/console/missions') ||
+    path === '/console' ||
-    path.startsWith('/console/store/temp/') ||
+    path === '/console/' ||
-    path.startsWith('/console/store/draft/') ||
+    path.startsWith('/console/missions') ||
-    path.startsWith('/console/control-tower');
+    path.startsWith('/console/store/temp/') ||
-  const isDev = typeof window !== 'undefined' && (import.meta.env?.DEV || process.env.NODE_ENV === 'development');
+    path.startsWith('/console/store/draft/') ||
-  if (
+    path.startsWith('/console/control-tower');
-    isDev &&
+  const isDev = typeof window !== 'undefined' && (import.meta.env?.DEV || process.env.NODE_ENV === 'development');
-    (path === '/app' ||
+  if (
-      path === '/app/' ||
+    isDev &&
-      path.startsWith('/app/missions') ||
+    (path === '/app' ||
-      path.startsWith('/app/store/temp/') ||
+      path === '/app/' ||
-      path.startsWith('/app/store/draft/') ||
+      path.startsWith('/app/missions') ||
-      path.startsWith('/app/console') ||
+      path.startsWith('/app/store/temp/') ||
-      path === '/console' ||
+      path.startsWith('/app/store/draft/') ||
-      path === '/console/' ||
+      path.startsWith('/app/console') ||
-      path.startsWith('/console/missions') ||
+      path === '/console' ||
-      path.startsWith('/console/store/temp/') ||
+      path === '/console/' ||
-      path.startsWith('/console/store/draft/') ||
+      path.startsWith('/console/missions') ||
-      path.startsWith('/console/control-tower'))
+      path.startsWith('/console/store/temp/') ||
-  ) {
+      path.startsWith('/console/store/draft/') ||
-    console.log('[RequireAuthOrGuest] console guard', {
+      path.startsWith('/console/control-tower'))
-      path,
+  ) {
-      isConsoleEntry,
+    console.log('[RequireAuthOrGuest] console guard', {
-      hasToken,
+      path,
-      userRole: user?.role ?? null,
+      isConsoleEntry,
-      isLoading,
+      hasToken,
-      allowed,
+      userRole: user?.role ?? null,
-      creatingGuest,
+      isLoading,
-      decision: allowed ? 'allowed' : creatingGuest ? 'creating_guest' : isConsoleEntry ? 'guest_allowed_console' : 'blocked',
+      allowed,
-    });
+      creatingGuest,
-  }
+      decision: allowed ? 'allowed' : creatingGuest ? 'creating_guest' : isConsoleEntry ? 'guest_allowed_console' : 'blocked',
-
+    });
-  // For console entry: never redirect to login; try guest in background but always show console
+  }
-  useEffect(() => {
+
-    if (allowed || hasToken) return;
+  // For console entry: never redirect to login; try guest in background but always show console
-    if (createdRef.current) return;
+  useEffect(() => {
-    createdRef.current = true;
+    if (allowed || hasToken) return;
-    setCreatingGuest(true);
+    if (createdRef.current) return;
-    getOrCreateGuestSession()
+    createdRef.current = true;
-      .then((res) => {
+    setCreatingGuest(true);
-        if (res.ok && res.token && typeof window !== 'undefined') {
+    getOrCreateGuestSession()
-          window.localStorage.setItem(storageKeys.bearer, res.token);
+      .then((res) => {
-          window.localStorage.setItem(storageKeys.adminToken, res.token);
+        if (res.ok && res.token && typeof window !== 'undefined') {
-          const userId = res.userId || res.tenantId || (res.user && res.user.id);
+          window.localStorage.setItem(storageKeys.bearer, res.token);
-          if (userId) setCanonicalContext({ tenantId: userId });
+          window.localStorage.setItem(storageKeys.adminToken, res.token);
-          window.dispatchEvent(new Event('authchange'));
+          const userId = res.userId || res.tenantId || (res.user && res.user.id);
-        } else {
+          if (userId) setCanonicalContext({ tenantId: userId });
-          createdRef.current = false;
+          window.dispatchEvent(new Event('authchange'));
-          if (!isConsoleEntry) {
+        } else {
-            const returnTo = encodeURIComponent(location.pathname + location.search);
+          createdRef.current = false;
-            navigate(`/login?returnTo=${returnTo}`, { replace: true });
+          if (!isConsoleEntry) {
-          }
+            const returnTo = encodeURIComponent(location.pathname + location.search);
-        }
+            navigate(`/login?returnTo=${returnTo}`, { replace: true });
-      })
+          }
-      .catch(() => {
+        }
-        createdRef.current = false;
+      })
-        if (!isConsoleEntry) {
+      .catch(() => {
-          const returnTo = encodeURIComponent(location.pathname + location.search);
+        createdRef.current = false;
-          navigate(`/login?returnTo=${returnTo}`, { replace: true });
+        if (!isConsoleEntry) {
-        }
+          const returnTo = encodeURIComponent(location.pathname + location.search);
-      })
+          navigate(`/login?returnTo=${returnTo}`, { replace: true });
-      .finally(() => { setCreatingGuest(false); });
+        }
-  }, [allowed, hasToken, isConsoleEntry, location.pathname, location.search, navigate]);
+      })
-
+      .finally(() => { setCreatingGuest(false); });
-  useEffect(() => {
+  }, [allowed, hasToken, isConsoleEntry, location.pathname, location.search, navigate]);
-    if (isConsoleEntry) return;
+
-    if (!allowed && !creatingGuest && !isLoading && hasToken && !user) {
+  useEffect(() => {
-      const returnTo = encodeURIComponent(location.pathname + location.search);
+    if (isConsoleEntry) return;
-      navigate(`/login?returnTo=${returnTo}`, { replace: true });
+    if (!allowed && !creatingGuest && !isLoading && hasToken && !user) {
-    }
+      const returnTo = encodeURIComponent(location.pathname + location.search);
-  }, [allowed, creatingGuest, isLoading, hasToken, user, isConsoleEntry, location.pathname, location.search, navigate]);
+      navigate(`/login?returnTo=${returnTo}`, { replace: true });
-
+    }
-  // Console entry: always show children (no redirect, no blocking). Guest creation runs in background.
+  }, [allowed, creatingGuest, isLoading, hasToken, user, isConsoleEntry, location.pathname, location.search, navigate]);
-  if (isConsoleEntry) return children;
+
-  if (creatingGuest || (hasToken && !user && isLoading)) return null;
+  // Console entry: always show children (no redirect, no blocking). Guest creation runs in background.
-  if (allowed) return children;
+  if (isConsoleEntry) return children;
-  if (hasToken && !user && !isLoading) return null;
+  if (creatingGuest || (hasToken && !user && isLoading)) return null;
-  return null;
+  if (allowed) return children;
-}
+  if (hasToken && !user && !isLoading) return null;
-
+  return null;
-export default function App() {
+}
-  const { role, user } = useSession();
+
-  const [tab, setTab] = useState("overview");
+export default function App() {
-  const loc = useLocation();
+  const { role, user } = useSession();
-
+  const [tab, setTab] = useState("overview");
-  // Calculate public page status BEFORE useEffect (needed for dependency)
+  const loc = useLocation();
-  // Include /app/store/temp/review and /app/store/draft/review (with or without mode=draft) so create-store flow never triggers auth redirect
+
-  const isTempStoreReview = loc.pathname.startsWith("/app/store/") && loc.pathname.includes("/review");
+  // Calculate public page status BEFORE useEffect (needed for dependency)
-  const isTempStoreReviewPath = /^\/app\/store\/temp\/review$/.test(loc.pathname);
+  // Include /app/store/temp/review and /app/store/draft/review (with or without mode=draft) so create-store flow never triggers auth redirect
-  const isDraftLineageReviewPath = /^\/app\/store\/draft\/review$/.test(loc.pathname);
+  const isTempStoreReview = loc.pathname.startsWith("/app/store/") && loc.pathname.includes("/review");
-  const isDocPublicView = /^\/doc\/[^/]+\/view$/.test(loc.pathname);
+  const isTempStoreReviewPath = /^\/app\/store\/temp\/review$/.test(loc.pathname);
-  const isCardLegacyDocView = /^\/card\/[^/]+\/view$/.test(loc.pathname);
+  const isDraftLineageReviewPath = /^\/app\/store\/draft\/review$/.test(loc.pathname);
-  const isPublicPage = loc.pathname === "/" ||
+  const isDocPublicView = /^\/doc\/[^/]+\/view$/.test(loc.pathname);
-                      loc.pathname === "/for-business" ||
+  const isCardLegacyDocView = /^\/card\/[^/]+\/view$/.test(loc.pathname);
-                      loc.pathname === "/for-sellers" ||
+  const isPublicPage = loc.pathname === "/" ||
-                      loc.pathname === "/create" ||
+                      loc.pathname === "/for-business" ||
-                      loc.pathname === "/features" ||
+                      loc.pathname === "/for-sellers" ||
-                      loc.pathname === "/about" ||
+                      loc.pathname === "/create" ||
-                      loc.pathname === "/pricing" ||
+                      loc.pathname === "/features" ||
-                      loc.pathname === "/signup" ||
+                      loc.pathname === "/about" ||
-                      loc.pathname.startsWith("/signup/") ||
+                      loc.pathname === "/pricing" ||
-                      loc.pathname === "/login" ||
+                      loc.pathname === "/signup" ||
-                      loc.pathname === "/verify-email" ||
+                      loc.pathname.startsWith("/signup/") ||
-                      loc.pathname === "/email-verified" ||
+                      loc.pathname === "/login" ||
-                      loc.pathname === "/account" ||
+                      loc.pathname === "/verify-email" ||
-                      loc.pathname.startsWith("/u/") ||
+                      loc.pathname === "/email-verified" ||
-                      loc.pathname === "/creators" ||
+                      loc.pathname === "/account" ||
-                      loc.pathname.startsWith("/creator/") ||
+                      loc.pathname.startsWith("/u/") ||
-                      loc.pathname.startsWith("/s/") ||
+                      loc.pathname === "/creators" ||
-                      loc.pathname.startsWith("/space/") ||
+                      loc.pathname.startsWith("/creator/") ||
-                      loc.pathname.startsWith("/preview/") ||
+                      loc.pathname.startsWith("/s/") ||
-                      loc.pathname.startsWith("/mi/job/") ||
+                      loc.pathname.startsWith("/space/") ||
-                      loc.pathname.startsWith("/q/") ||
+                      loc.pathname.startsWith("/preview/") ||
-                      loc.pathname.startsWith("/promo/") ||
+                      loc.pathname.startsWith("/mi/job/") ||
-                      loc.pathname.startsWith("/p/promo/") ||
+                      loc.pathname.startsWith("/q/") ||
-                      isDocPublicView ||
+                      loc.pathname.startsWith("/promo/") ||
-                      isCardLegacyDocView ||
+                      loc.pathname.startsWith("/p/promo/") ||
-                      (isTempStoreReview && (isTempStoreReviewPath || isDraftLineageReviewPath || new URLSearchParams(loc.search).get("mode") === "draft"));
+                      isDocPublicView ||
-
+                      isCardLegacyDocView ||
-  // Initialize auth state on app mount (check auth status once on startup)
+                      (isTempStoreReview && (isTempStoreReviewPath || isDraftLineageReviewPath || new URLSearchParams(loc.search).get("mode") === "draft"));
-  // SKIP for public pages to avoid triggering /api/auth/me calls
+
-  useEffect(() => {
+  // Initialize auth state on app mount (check auth status once on startup)
-    // Don't check auth for public pages (including /mi/job/:jobId)
+  // SKIP for public pages to avoid triggering /api/auth/me calls
-    if (isPublicPage) {
+  useEffect(() => {
-      return;
+    // Don't check auth for public pages (including /mi/job/:jobId)
-    }
+    if (isPublicPage) {
-    
+      return;
-    import('./hooks/useAuth').then(({ checkAuthStatus }) => {
+    }
-      checkAuthStatus();
+    
-    });
+    import('./hooks/useAuth').then(({ checkAuthStatus }) => {
-  }, [isPublicPage]);
+      checkAuthStatus();
-
+    });
-  // Dev-only fallback API key for legacy device SSE demos; production uses stream-token / VITE_SSE_STREAM_KEY.
+  }, [isPublicPage]);
-  useEffect(() => {
+
-    if (import.meta.env.PROD) return;
+  // Dev-only fallback API key for legacy device SSE demos; production uses stream-token / VITE_SSE_STREAM_KEY.
-    const tokens = getStorageTokens();
+  useEffect(() => {
-    if (!tokens.apiKey) {
+    if (import.meta.env.PROD) return;
-      localStorage.setItem(storageKeys.apiKey, 'admin');
+    const tokens = getStorageTokens();
-    }
+    if (!tokens.apiKey) {
-  }, []);
+      localStorage.setItem(storageKeys.apiKey, 'admin');
-
+    }
-  const isLogin = loc.pathname === "/login";
+  }, []);
-  const isPerformer = loc.pathname.startsWith("/app/performer");
+
-  const isDevicePlayer = loc.pathname === "/device/player";
+  const isLogin = loc.pathname === "/login";
-  /** Light commercial artifact feed at `/` (not legacy dark frontscreen). */
+  const isPerformer = loc.pathname.startsWith("/app/performer");
-  const isPublicFeedHome = loc.pathname === "/";
+  const isDevicePlayer = loc.pathname === "/device/player";
-  const isLegacyFrontscreen =
+  /** Light commercial artifact feed at `/` (not legacy dark frontscreen). */
-    loc.pathname === "/frontscreen" ||
+  const isPublicFeedHome = loc.pathname === "/";
-    loc.pathname === "/creators" ||
+  const isLegacyFrontscreen =
-    loc.pathname === "/search" ||
+    loc.pathname === "/frontscreen" ||
-    loc.pathname === "/frontscreen/stores" ||
+    loc.pathname === "/creators" ||
-    loc.pathname === "/food" ||
+    loc.pathname === "/search" ||
-    loc.pathname.startsWith("/food/");
+    loc.pathname === "/frontscreen/stores" ||
-  const isFeedBusinessChannel =
+    loc.pathname === "/food" ||
-    /^\/s\/[^/]+/.test(loc.pathname) &&
+    loc.pathname.startsWith("/food/");
-    new URLSearchParams(loc.search).get("from") === "feed";
+  const isFeedBusinessChannel =
-  const isFrontscreen = isPublicFeedHome || isLegacyFrontscreen || isFeedBusinessChannel;
+    /^\/s\/[^/]+/.test(loc.pathname) &&
-  const isBackOffice = loc.pathname.startsWith("/app/back");
+    new URLSearchParams(loc.search).get("from") === "feed";
-  const isConsole =
+  const isFrontscreen = isPublicFeedHome || isLegacyFrontscreen || isFeedBusinessChannel;
-    loc.pathname === "/app" ||
+  const isBackOffice = loc.pathname.startsWith("/app/back");
-    loc.pathname === "/app/" ||
+  const isConsole =
-    loc.pathname.startsWith("/app/missions") ||
+    loc.pathname === "/app" ||
-    loc.pathname.startsWith("/app/console") ||
+    loc.pathname === "/app/" ||
-    loc.pathname === "/console" ||
+    loc.pathname.startsWith("/app/missions") ||
-    loc.pathname === "/console/" ||
+    loc.pathname.startsWith("/app/console") ||
-    loc.pathname.startsWith("/console/");
+    loc.pathname.startsWith("/app/development") ||
-  /** Store dashboard uses DashboardShell + ConsoleSidebar; do not wrap in PageShell with legacy Sidebar. */
+    loc.pathname === "/console" ||
-  const isBusiness = loc.pathname === "/business" || loc.pathname.startsWith("/business/");
+    loc.pathname === "/console/" ||
-  const isBusinessHealth =
+    loc.pathname.startsWith("/console/");
-    loc.pathname === "/business-health" || loc.pathname.startsWith("/business-health/");
+  /** Store dashboard uses DashboardShell + ConsoleSidebar; do not wrap in PageShell with legacy Sidebar. */
-  const isCreatorStudio = loc.pathname === "/creator-studio" || loc.pathname.startsWith("/creator-studio/");
+  const isBusiness = loc.pathname === "/business" || loc.pathname.startsWith("/business/");
-  const isPublishReview = /^\/app\/store\/[^/]+\/publish-review$/.test(loc.pathname);
+  const isBusinessHealth =
-
+    loc.pathname === "/business-health" || loc.pathname.startsWith("/business-health/");
-  const sidebar = isPublishReview ? null : <Sidebar />;
+  const isCreatorStudio = loc.pathname === "/creator-studio" || loc.pathname.startsWith("/creator-studio/");
-
+  const isPublishReview = /^\/app\/store\/[^/]+\/publish-review$/.test(loc.pathname);
-  const rightbar = null;
+
-
+  const sidebar = isPublishReview ? null : <Sidebar />;
-  const routes = (
+
-    <Routes>
+  const rightbar = null;
-      <Route path="/doc/:docId/view" element={<DocViewPage />} />
+
-      <Route path="/card/:cardId/view" element={<CardViewPage />} />
+  const routes = (
-      {/* Store review: single param route so storeId is always in useParams(); /app/store/temp/review matches with storeId=temp (StoreReviewGate allows guests for temp) */}
+    <Routes>
-      {/* Back Office (single parent with nested routes) */}
+      <Route path="/doc/:docId/view" element={<DocViewPage />} />
-      <Route
+      <Route path="/card/:cardId/view" element={<CardViewPage />} />
-        path="/app/back"
+      {/* Store review: single param route so storeId is always in useParams(); /app/store/temp/review matches with storeId=temp (StoreReviewGate allows guests for temp) */}
-        element={
+      {/* Back Office (single parent with nested routes) */}
-          <RequireAuth>
+      <Route
-            <BackOffice />
+        path="/app/back"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      >
+            <BackOffice />
-        {/* Mission chat - specific path first so it matches before any catch-all */}
+          </RequireAuth>
-        <Route path="missions/:missionId/chat" element={<AgentChatPage />} />
+        }
-        {/* Redirect /app/back/missions/:id (no /chat) to .../chat so the chat page always loads */}
+      >
-        <Route
+        {/* Mission chat - specific path first so it matches before any catch-all */}
-          path="missions/:missionId"
+        <Route path="missions/:missionId/chat" element={<AgentChatPage />} />
-          element={<Navigate to="chat" replace />}
+        {/* Redirect /app/back/missions/:id (no /chat) to .../chat so the chat page always loads */}
-        />
+        <Route
-        <Route index element={<Dashboard />} />
+          path="missions/:missionId"
-        <Route path="dashboard" element={<Dashboard />} />
+          element={<Navigate to="chat" replace />}
-        <Route path="performer" element={<Performer />} />
+        />
-        <Route path="enhanced-performer/*" element={<EnhancedPerformer />} />
+        <Route index element={<Dashboard />} />
-        <Route path="screens" element={<ScreensPage />} />
+        <Route path="dashboard" element={<Dashboard />} />
-        {/* Minimal creatives debug view for end-to-end OpenClaw campaign content */}
+        <Route path="performer" element={<Performer />} />
-        <Route
+        <Route path="enhanced-performer/*" element={<EnhancedPerformer />} />
-          path="campaigns/:campaignId/creatives"
+        <Route path="screens" element={<ScreensPage />} />
-          element={<DebugCampaignCreatives />}
+        {/* Minimal creatives debug view for end-to-end OpenClaw campaign content */}
-        />
+        <Route
-        <Route path="campaigns/*" element={<Campaigns />} />
+          path="campaigns/:campaignId/creatives"
-        <Route path="content" element={<Content />} />
+          element={<DebugCampaignCreatives />}
-        <Route path="products" element={<Products />} />
+        />
-        <Route path="orders" element={<Orders />} />
+        <Route path="campaigns/*" element={<Campaigns />} />
-        <Route path="roles" element={<Roles />} />
+        <Route path="content" element={<Content />} />
-        <Route path="api-keys" element={<ApiKeysPage />} />
+        <Route path="products" element={<Products />} />
-        <Route path="control-hub" element={<ControlHub />} />
+        <Route path="orders" element={<Orders />} />
-        <Route path="loyalty/from-card" element={<LoyaltyFromCardPage />} />
+        <Route path="roles" element={<Roles />} />
-        <Route path="agent-chat-test" element={<AgentChatTestPage />} />
+        <Route path="api-keys" element={<ApiKeysPage />} />
-        <Route path="layout-studio" element={<LayoutStudio />} />
+        <Route path="control-hub" element={<ControlHub />} />
-        <Route path="threads" element={<ThreadListView />} />
+        <Route path="loyalty/from-card" element={<LoyaltyFromCardPage />} />
-        <Route path="threads/:threadId" element={<ThreadChatView />} />
+        <Route path="agent-chat-test" element={<AgentChatTestPage />} />
-        {/* Fallback under /app/back */}
+        <Route path="layout-studio" element={<LayoutStudio />} />
-        <Route path="*" element={<Navigate to="dashboard" replace />} />
+        <Route path="threads" element={<ThreadListView />} />
-      </Route>
+        <Route path="threads/:threadId" element={<ThreadChatView />} />
-
+        {/* Fallback under /app/back */}
-      {/* Control Tower — explicit top-level paths (avoid * → /app/back fallback missing nested /app/console/*) */}
+        <Route path="*" element={<Navigate to="dashboard" replace />} />
-      <Route
+      </Route>
-        path="/app/console/control-tower"
+
-        element={
+      {/* Control Tower — explicit top-level paths (avoid * → /app/back fallback missing nested /app/console/*) */}
-          <RequireAuthOrGuest>
+      <Route
-            <GuestPerformerEntryGuard>
+        path="/app/console/control-tower"
-              <ConsoleShell />
+        element={
-            </GuestPerformerEntryGuard>
+          <RequireAuthOrGuest>
-          </RequireAuthOrGuest>
+            <GuestPerformerEntryGuard>
-        }
+              <ConsoleShell />
-      >
+            </GuestPerformerEntryGuard>
-        <Route index element={<ControlTowerPage />} />
+          </RequireAuthOrGuest>
-      </Route>
+        }
-      <Route path="/app/console" element={<Navigate to="/app/console/control-tower" replace />} />
+      >
-      <Route
+        <Route index element={<ControlTowerPage />} />
-        path="/console/control-tower"
+      </Route>
-        element={
+      <Route path="/app/console" element={<Navigate to="/app/console/control-tower" replace />} />
-          <RequireAuthOrGuest>
+      <Route
-            <GuestPerformerEntryGuard>
+        path="/console/control-tower"
-              <ConsoleShell />
+        element={
-            </GuestPerformerEntryGuard>
+          <RequireAuthOrGuest>
-          </RequireAuthOrGuest>
+            <GuestPerformerEntryGuard>
-        }
+              <ConsoleShell />
-      >
+            </GuestPerformerEntryGuard>
-        <Route index element={<ControlTowerPage />} />
+          </RequireAuthOrGuest>
-      </Route>
+        }
-
+      >
-      {/* Platform Admin — legacy /admin redirects to Account Management */}
+        <Route index element={<ControlTowerPage />} />
-      <Route
+      </Route>
-        path="/admin"
+
-        element={
+      {/* Platform Admin — legacy /admin redirects to Account Management */}
-          <PlatformAdminRoute>
+      <Route
-            <Navigate to="/control-center/account-management" replace />
+        path="/admin"
-          </PlatformAdminRoute>
+        element={
-        }
+          <PlatformAdminRoute>
-      />
+            <Navigate to="/control-center/account-management" replace />
-      <Route
+          </PlatformAdminRoute>
-        path="/admin/discovery"
+        }
-        element={
+      />
-          <SuperAdminRoute>
+      <Route
-            <DiscoveryAdminPage />
+        path="/admin/discovery"
-          </SuperAdminRoute>
+        element={
-        }
+          <SuperAdminRoute>
-      />
+            <DiscoveryAdminPage />
-
+          </SuperAdminRoute>
-      {/* Discovery Governance — platform admin business ingestion workflow */}
+        }
-      <Route
+      />
-        path="/control-center/qa-review"
+
-        element={
+      {/* Discovery Governance — platform admin business ingestion workflow */}
-          <PlatformAdminRoute>
+      <Route
-            <QaReviewPage />
+        path="/control-center/qa-review"
-          </PlatformAdminRoute>
+        element={
-        }
+          <PlatformAdminRoute>
-      />
+            <QaReviewPage />
-      <Route
+          </PlatformAdminRoute>
-        path="/control-center/claims"
+        }
-        element={
+      />
-          <PlatformAdminRoute>
+      <Route
-            <ClaimsReviewPage />
+        path="/control-center/claims"
-          </PlatformAdminRoute>
+        element={
-        }
+          <PlatformAdminRoute>
-      />
+            <ClaimsReviewPage />
-      <Route
+          </PlatformAdminRoute>
-        path="/control-center/funnel"
+        }
-        element={
+      />
-          <PlatformAdminRoute>
+      <Route
-            <ActivationFunnelPage />
+        path="/control-center/funnel"
-          </PlatformAdminRoute>
+        element={
-        }
+          <PlatformAdminRoute>
-      />
+            <ActivationFunnelPage />
-      <Route
+          </PlatformAdminRoute>
-        path="/control-center/growth"
+        }
-        element={
+      />
-          <PlatformAdminRoute>
+      <Route
-            <GrowthCommandCenterPage />
+        path="/control-center/growth"
-          </PlatformAdminRoute>
+        element={
-        }
+          <PlatformAdminRoute>
-      />
+            <GrowthCommandCenterPage />
-      <Route
+          </PlatformAdminRoute>
-        path="/control-center/discovery-center"
+        }
-        element={
+      />
-          <PlatformAdminRoute>
+      <Route
-            <DiscoveryCenterPage />
+        path="/control-center/discovery-center"
-          </PlatformAdminRoute>
+        element={
-        }
+          <PlatformAdminRoute>
-      />
+            <DiscoveryCenterPage />
-      <Route
+          </PlatformAdminRoute>
-        path="/control-center/discovery-rollback"
+        }
-        element={
+      />
-          <PlatformAdminRoute>
+      <Route
-            <DiscoveryRollbackPage />
+        path="/control-center/discovery-rollback"
-          </PlatformAdminRoute>
+        element={
-        }
+          <PlatformAdminRoute>
-      />
+            <DiscoveryRollbackPage />
-      <Route
+          </PlatformAdminRoute>
-        path="/control-center/language"
+        }
-        element={
+      />
-          <PlatformAdminRoute>
+      <Route
-            <LanguageAgentPage />
+        path="/control-center/language"
-          </PlatformAdminRoute>
+        element={
-        }
+          <PlatformAdminRoute>
-      />
+            <LanguageAgentPage />
-      <Route
+          </PlatformAdminRoute>
-        path="/control-center/account-management"
+        }
-        element={
+      />
-          <PlatformAdminRoute>
+      <Route
-            <AccountManagementPage />
+        path="/control-center/account-management"
-          </PlatformAdminRoute>
+        element={
-        }
+          <PlatformAdminRoute>
-      />
+            <AccountManagementPage />
-
+          </PlatformAdminRoute>
-      {/* Homepage — light commercial vertical artifact feed (Phase 2A). Legacy dark frontscreen at /frontscreen. */}
+        }
-      <Route
+      />
-        path="/"
+
-        element={
+      {/* Homepage — light commercial vertical artifact feed (Phase 2A). Legacy dark frontscreen at /frontscreen. */}
-          <React.Suspense fallback={<PublicFeedInstantShell />}>
+      <Route
-            <PublicHomeFeed />
+        path="/"
-          </React.Suspense>
+        element={
-        }
+          <React.Suspense fallback={<PublicFeedInstantShell />}>
-      />
+            <PublicHomeFeed />
-      {/* Unified discovery search — stores, products, services, menus, offers */}
+          </React.Suspense>
-      <Route
+        }
-        path="/search"
+      />
-        element={
+      {/* Unified discovery search — stores, products, services, menus, offers */}
-          <React.Suspense fallback={<PublicFeedInstantShell />}>
+      <Route
-            <DiscoverySearchPage />
+        path="/search"
-          </React.Suspense>
+        element={
-        }
+          <React.Suspense fallback={<PublicFeedInstantShell />}>
-      />
+            <DiscoverySearchPage />
-      <Route
+          </React.Suspense>
-        path="/creators"
+        }
-        element={
+      />
-          <React.Suspense fallback={<PublicFeedInstantShell />}>
+      <Route
-            <CreatorsFeedPage />
+        path="/creators"
-          </React.Suspense>
+        element={
-        }
+          <React.Suspense fallback={<PublicFeedInstantShell />}>
-      />
+            <CreatorsFeedPage />
-      <Route
+          </React.Suspense>
-        path="/creator/:username"
+        }
-        element={
+      />
-          <React.Suspense fallback={<PublicFeedInstantShell />}>
+      <Route
-            <CreatorProfilePage />
+        path="/creator/:username"
-          </React.Suspense>
+        element={
-        }
+          <React.Suspense fallback={<PublicFeedInstantShell />}>
-      />
+            <CreatorProfilePage />
-      <Route
+          </React.Suspense>
-        path="/creator-studio"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <React.Suspense fallback={<PublicFeedInstantShell />}>
+        path="/creator-studio"
-              <CreatorStudioLayout />
+        element={
-            </React.Suspense>
+          <RequireAuth>
-          </RequireAuth>
+            <React.Suspense fallback={<PublicFeedInstantShell />}>
-        }
+              <CreatorStudioLayout />
-      >
+            </React.Suspense>
-        <Route index element={<CreatorStudioOverview />} />
+          </RequireAuth>
-        <Route path="content" element={<CreatorStudioContentPage />} />
+        }
-        <Route path="articles" element={<CreatorStudioContentPage filterType="ARTICLE" />} />
+      >
-        <Route path="videos" element={<CreatorStudioContentPage filterType="VIDEO" />} />
+        <Route index element={<CreatorStudioOverview />} />
-        <Route path="livestreams" element={<CreatorStudioContentPage filterType="LIVESTREAM" />} />
+        <Route path="content" element={<CreatorStudioContentPage />} />
-        <Route path="drafts" element={<CreatorStudioContentPage statusFilter="draft" />} />
+        <Route path="articles" element={<CreatorStudioContentPage filterType="ARTICLE" />} />
-        <Route path="analytics" element={<CreatorStudioAnalyticsPage />} />
+        <Route path="videos" element={<CreatorStudioContentPage filterType="VIDEO" />} />
-        <Route path="progress" element={<CreatorStudioProgressPage />} />
+        <Route path="livestreams" element={<CreatorStudioContentPage filterType="LIVESTREAM" />} />
-        <Route path="settings" element={<CreatorStudioSettingsPage />} />
+        <Route path="drafts" element={<CreatorStudioContentPage statusFilter="draft" />} />
-      </Route>
+        <Route path="analytics" element={<CreatorStudioAnalyticsPage />} />
-      {/* Space (Phase 1 foundation) — public, additive */}
+        <Route path="progress" element={<CreatorStudioProgressPage />} />
-      <Route path="/space/:spaceId" element={<SpacePage />} />
+        <Route path="settings" element={<CreatorStudioSettingsPage />} />
-      {/* AI-native business entry runway (Phase 3A) */}
+      </Route>
-      <Route path="/for-business" element={<BusinessEntryRuntimePage />} />
+      {/* Space (Phase 1 foundation) — public, additive */}
-      <Route path="/for-sellers" element={<BusinessEntryRuntimePage />} />
+      <Route path="/space/:spaceId" element={<SpacePage />} />
-      {/* Business Discovery / Ingestion (Phase 1) */}
+      {/* AI-native business entry runway (Phase 3A) */}
-      <Route path="/discover-business" element={<BusinessDiscoveryPage />} />
+      <Route path="/for-business" element={<BusinessEntryRuntimePage />} />
-      <Route
+      <Route path="/for-sellers" element={<BusinessEntryRuntimePage />} />
-        path="/business/:slug"
+      {/* Business Discovery / Ingestion (Phase 1) */}
-        element={
+      <Route path="/discover-business" element={<BusinessDiscoveryPage />} />
-          <BusinessPublicOrBuilderRoute
+      <Route
-            dashboard={
+        path="/business/:slug"
-              <RequireAuth>
+        element={
-                <RequireFeature feature="business_builder_v1">
+          <BusinessPublicOrBuilderRoute
-                  <BusinessDashboard />
+            dashboard={
-                </RequireFeature>
+              <RequireAuth>
-              </RequireAuth>
+                <RequireFeature feature="business_builder_v1">
-            }
+                  <BusinessDashboard />
-          />
+                </RequireFeature>
-        }
+              </RequireAuth>
-      />
+            }
-      <Route path="/activate-business/:seedId" element={<ActivateBusinessRouteGate />} />
+          />
-      <Route path="/business-health/:slug" element={<BusinessHealthReportPage />} />
+        }
-      <Route
+      />
-        path="/business-health"
+      <Route path="/activate-business/:seedId" element={<ActivateBusinessRouteGate />} />
-        element={
+      <Route path="/business-health/:slug" element={<BusinessHealthReportPage />} />
-          <div className="mx-auto max-w-lg px-4 py-16 text-center">
+      <Route
-            <p className="text-lg font-semibold text-slate-900">Business Health Report</p>
+        path="/business-health"
-            <p className="mt-2 text-sm text-slate-600">
+        element={
-              Open a discovered business at /business-health/your-slug or /business/your-slug
+          <div className="mx-auto max-w-lg px-4 py-16 text-center">
-            </p>
+            <p className="text-lg font-semibold text-slate-900">Business Health Report</p>
-          </div>
+            <p className="mt-2 text-sm text-slate-600">
-        }
+              Open a discovered business at /business-health/your-slug or /business/your-slug
-      />
+            </p>
-      <Route path="/claim-business/:claimIntentId" element={<ClaimBusinessRoute />} />
+          </div>
-      {/* Create: public /create with AI prompt + Generate (PR1) */}
+        }
-      <Route
+      />
-        path="/create"
+      <Route path="/claim-business/:claimIntentId" element={<ClaimBusinessRoute />} />
-        element={<CreatePage />}
+      {/* Create: public /create with AI prompt + Generate (PR1) */}
-      />
+      <Route
-      {/* Backward compat: /features → seller landing #create section */}
+        path="/create"
-      <Route
+        element={<CreatePage />}
-        path="/features"
+      />
-        element={<Navigate to="/for-business" replace />}
+      {/* Backward compat: /features → seller landing #create section */}
-      />
+      <Route
-      {/* MI Job Status Page */}
+        path="/features"
-      <Route
+        element={<Navigate to="/for-business" replace />}
-        path="/mi/job/:jobId"
+      />
-        element={<MiJobStatusPage />}
+      {/* MI Job Status Page */}
-      />
+      <Route
-      {/* Canonical Store Review: guests allowed for pre-publish lineage (temp|draft) via StoreReviewGate; real store IDs require auth */}
+        path="/mi/job/:jobId"
-      <Route path="/app/store/draft/preview" element={<DraftStorePreviewAliasRedirect />} />
+        element={<MiJobStatusPage />}
-      <Route
+      />
-        path="/app/store/:storeId/review"
+      {/* Canonical Store Review: guests allowed for pre-publish lineage (temp|draft) via StoreReviewGate; real store IDs require auth */}
-        element={
+      <Route path="/app/store/draft/preview" element={<DraftStorePreviewAliasRedirect />} />
-          <StoreReviewGate>
+      <Route
-            <StoreReviewPage />
+        path="/app/store/:storeId/review"
-          </StoreReviewGate>
+        element={
-        }
+          <StoreReviewGate>
-      />
+            <StoreReviewPage />
-      {/* Read-only Publish Store Review - same content as draft, no edit/publish UI */}
+          </StoreReviewGate>
-      <Route
+        }
-        path="/app/store/:storeId/publish-review"
+      />
-        element={
+      {/* Read-only Publish Store Review - same content as draft, no edit/publish UI */}
-          <RequireAuth>
+      <Route
-            <StorePublishReviewPage />
+        path="/app/store/:storeId/publish-review"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <StorePublishReviewPage />
-      {/* Canonical preview route - storeId-based (lazy-loaded to avoid MIME type issues with .tsx in dev) */}
+          </RequireAuth>
-      <Route
+        }
-        path="/preview/store/:storeId"
+      />
-        element={
+      {/* Canonical preview route - storeId-based (lazy-loaded to avoid MIME type issues with .tsx in dev) */}
-          <React.Suspense fallback={
+      <Route
-            <div className="flex min-h-screen items-center justify-center bg-background">
+        path="/preview/store/:storeId"
-              <div className="text-muted-foreground">Loading store…</div>
+        element={
-            </div>
+          <React.Suspense fallback={
-          }>
+            <div className="flex min-h-screen items-center justify-center bg-background">
-            <StorePreviewPage />
+              <div className="text-muted-foreground">Loading store…</div>
-          </React.Suspense>
+            </div>
-        }
+          }>
-      />
+            <StorePreviewPage />
-      <Route
+          </React.Suspense>
-        path="/preview/website/:draftId"
+        }
-        element={
+      />
-          <CanonicalStorefrontRenderer mode="preview">
+      <Route
-            <React.Suspense fallback={
+        path="/preview/website/:draftId"
-              <div className="flex min-h-screen items-center justify-center bg-background">
+        element={
-                <div className="text-muted-foreground">Loading website…</div>
+          <CanonicalStorefrontRenderer mode="preview">
-              </div>
+            <React.Suspense fallback={
-            }>
+              <div className="flex min-h-screen items-center justify-center bg-background">
-              <WebsitePreviewPage />
+                <div className="text-muted-foreground">Loading website…</div>
-            </React.Suspense>
+              </div>
-          </CanonicalStorefrontRenderer>
+            }>
-        }
+              <WebsitePreviewPage />
-      />
+            </React.Suspense>
-      <Route
+          </CanonicalStorefrontRenderer>
-        path="/w/:draftId"
+        }
-        element={
+      />
-          <CanonicalStorefrontRenderer mode="preview">
+      <Route
-            <React.Suspense fallback={
+        path="/w/:draftId"
-              <div className="flex min-h-screen items-center justify-center bg-background">
+        element={
-                <div className="text-muted-foreground">Loading website…</div>
+          <CanonicalStorefrontRenderer mode="preview">
-              </div>
+            <React.Suspense fallback={
-            }>
+              <div className="flex min-h-screen items-center justify-center bg-background">
-              <WebsitePreviewPage />
+                <div className="text-muted-foreground">Loading website…</div>
-            </React.Suspense>
+              </div>
-          </CanonicalStorefrontRenderer>
+            }>
-        }
+              <WebsitePreviewPage />
-      />
+            </React.Suspense>
-      {/* Service store booking (public) – 3-step flow: services → staff/date/time → checkout */}
+          </CanonicalStorefrontRenderer>
-      <Route
+        }
-        path="/store-booking/:storeId"
+      />
-        element={
+      {/* Service store booking (public) – 3-step flow: services → staff/date/time → checkout */}
-          <React.Suspense fallback={
+      <Route
-            <div className="flex min-h-screen items-center justify-center bg-background">
+        path="/store-booking/:storeId"
-              <div className="text-muted-foreground">Loading booking…</div>
+        element={
-            </div>
+          <React.Suspense fallback={
-          }>
+            <div className="flex min-h-screen items-center justify-center bg-background">
-            <BookingPage />
+              <div className="text-muted-foreground">Loading booking…</div>
-          </React.Suspense>
+            </div>
-        }
+          }>
-      />
+            <BookingPage />
-      <Route
+          </React.Suspense>
-        path="/booking-success"
+        }
-        element={
+      />
-          <React.Suspense fallback={
+      <Route
-            <div className="flex min-h-screen items-center justify-center bg-background">
+        path="/booking-success"
-              <div className="text-muted-foreground">Loading…</div>
+        element={
-            </div>
+          <React.Suspense fallback={
-          }>
+            <div className="flex min-h-screen items-center justify-center bg-background">
-            <BookingSuccessPage />
+              <div className="text-muted-foreground">Loading…</div>
-          </React.Suspense>
+            </div>
-        }
+          }>
-      />
+            <BookingSuccessPage />
-      {/* Read-only draft preview for guests (no auth); redirects to /preview/:jobId */}
+          </React.Suspense>
-      <Route
+        }
-        path="/preview/draft"
+      />
-        element={
+      {/* Read-only draft preview for guests (no auth); redirects to /preview/:jobId */}
-          <React.Suspense fallback={
+      <Route
-            <div className="flex min-h-screen items-center justify-center bg-background">
+        path="/preview/draft"
-              <div className="text-muted-foreground">Loading…</div>
+        element={
-            </div>
+          <React.Suspense fallback={
-          }>
+            <div className="flex min-h-screen items-center justify-center bg-background">
-            <PreviewDraftPage />
+              <div className="text-muted-foreground">Loading…</div>
-          </React.Suspense>
+            </div>
-        }
+          }>
-      />
+            <PreviewDraftPage />
-      {/* Legacy preview route - supports draftId/jobId */}
+          </React.Suspense>
-      <Route
+        }
-        path="/preview/:draftId"
+      />
-        element={
+      {/* Legacy preview route - supports draftId/jobId */}
-          <React.Suspense fallback={
+      <Route
-            <div className="flex min-h-screen items-center justify-center bg-background">
+        path="/preview/:draftId"
-              <div className="text-muted-foreground">Loading store…</div>
+        element={
-            </div>
+          <React.Suspense fallback={
-          }>
+            <div className="flex min-h-screen items-center justify-center bg-background">
-            <StorePreviewPage />
+              <div className="text-muted-foreground">Loading store…</div>
-          </React.Suspense>
+            </div>
-        }
+          }>
-      />
+            <StorePreviewPage />
-      <Route
+          </React.Suspense>
-        path="/about"
+        }
-        element={<AboutPage />}
+      />
-      />
+      <Route
-      <Route
+        path="/about"
-        path="/pricing"
+        element={<AboutPage />}
-        element={<PricingPage />}
+      />
-      />
+      <Route
-      
+        path="/pricing"
-      {/* Public User Profile */}
+        element={<PricingPage />}
-      <Route
+      />
-        path="/u/:handle"
+      
-        element={<PublicProfilePage />}
+      {/* Public User Profile */}
-      />
+      <Route
-      
+        path="/u/:handle"
-      {/* Legacy public feed — redirect to canonical `/` and `/s/:slug` */}
+        element={<PublicProfilePage />}
-      <Route path="/feed" element={<LegacyFeedHomeRedirect />} />
+      />
-      <Route path="/feed/:slug" element={<LegacyFeedSlugRedirect />} />
+      
-      <Route path="/card/:slug" element={<LegacyCardSlugRedirect />} />
+      {/* Legacy public feed — redirect to canonical `/` and `/s/:slug` */}
-      <Route path="/card/:slug/store" element={<LegacyCardSlugRedirect />} />
+      <Route path="/feed" element={<LegacyFeedHomeRedirect />} />
-      
+      <Route path="/feed/:slug" element={<LegacyFeedSlugRedirect />} />
-      {/* Public Greeting Card */}
+      <Route path="/card/:slug" element={<LegacyCardSlugRedirect />} />
-      <Route
+      <Route path="/card/:slug/store" element={<LegacyCardSlugRedirect />} />
-        path="/greet/:shareSlug"
+      
-        element={<GreetingCardPublicPage />}
+      {/* Public Greeting Card */}
-      />
+      <Route
-      
+        path="/greet/:shareSlug"
-      {/* Public Promo Landing Page (MI) - more specific first */}
+        element={<GreetingCardPublicPage />}
-      <Route
+      />
-        path="/p/promo/:publicId"
+      
-        element={<PromoLandingPage />}
+      {/* Public Promo Landing Page (MI) - more specific first */}
-      />
+      <Route
-      {/* Alias for launch_campaign / QR (core uses PUBLIC_BASE_URL/promo/:publicId) */}
+        path="/p/promo/:publicId"
-      <Route
+        element={<PromoLandingPage />}
-        path="/promo/:publicId"
+      />
-        element={<PromoLandingPage />}
+      {/* Alias for launch_campaign / QR (core uses PUBLIC_BASE_URL/promo/:publicId) */}
-      />
+      <Route
-      {/* Phase 1 Scan & Redeem promo landing - NO AUTH - /p/:promoId */}
+        path="/promo/:publicId"
-      <Route
+        element={<PromoLandingPage />}
-        path="/p/:promoId"
+      />
-        element={<PromoScanRedeemLandingPage />}
+      {/* Phase 1 Scan & Redeem promo landing - NO AUTH - /p/:promoId */}
-      />
+      <Route
-      
+        path="/p/:promoId"
-      {/* Public Promo Registration Page - NO AUTH REQUIRED */}
+        element={<PromoScanRedeemLandingPage />}
-      <Route
+      />
-        path="/r/:publicId"
+      
-        element={<PublicPromoRegisterPage />}
+      {/* Public Promo Registration Page - NO AUTH REQUIRED */}
-      />
+      <Route
-      
+        path="/r/:publicId"
-      {/* MI Object Landing Page - Stable QR code landing with chat - NO AUTH REQUIRED */}
+        element={<PublicPromoRegisterPage />}
-      <Route
+      />
-        path="/mi/o/:publicId"
+      
-        element={<MIObjectLandingPage />}
+      {/* MI Object Landing Page - Stable QR code landing with chat - NO AUTH REQUIRED */}
-      />
+      <Route
-      
+        path="/mi/o/:publicId"
-      {/* Promo under Dynamic QR prefix: /q/:code/promo/:publicId (tenant/slug segment before promo) */}
+        element={<MIObjectLandingPage />}
-      <Route
+      />
-        path="/q/:qrSlug/promo/:publicId"
+      
-        element={<PromoLandingPage />}
+      {/* Promo under Dynamic QR prefix: /q/:code/promo/:publicId (tenant/slug segment before promo) */}
-      />
+      <Route
-      {/* Print Bag Landing Page - Mobile QR code landing for print bags - NO AUTH REQUIRED */}
+        path="/q/:qrSlug/promo/:publicId"
-      <Route
+        element={<PromoLandingPage />}
-        path="/q/:publicCode"
+      />
-        element={<PrintBagLandingPage />}
+      {/* Print Bag Landing Page - Mobile QR code landing for print bags - NO AUTH REQUIRED */}
-      />
+      <Route
-      
+        path="/q/:publicCode"
-      {/* Public slug URLs: mini website (sections/theme) → WebsitePreviewPage; else storefront / profile (PublicStorePage). No PageShell — see isPublicPage /store/ */}
+        element={<PrintBagLandingPage />}
-      <Route
+      />
-        path="/s/:slug"
+      
-        element={<PublicStoreSlugRoute />}
+      {/* Public slug URLs: mini website (sections/theme) → WebsitePreviewPage; else storefront / profile (PublicStorePage). No PageShell — see isPublicPage /store/ */}
-      />
+      <Route
-      <Route
+        path="/s/:slug"
-        path="/store/:slug"
+        element={<PublicStoreSlugRoute />}
-        element={<PublicStoreSlugRoute />}
+      />
-      />
+      <Route
-      
+        path="/store/:slug"
-      {/* Phase 1 Dashboard Routes */}
+        element={<PublicStoreSlugRoute />}
-      <Route
+      />
-        path="/dashboard"
+      
-        element={
+      {/* Phase 1 Dashboard Routes */}
-          <RequireAuth>
+      <Route
-            <DashboardHome />
+        path="/dashboard"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <DashboardHome />
-      <Route
+          </RequireAuth>
-        path="/dashboard/stores/:storeId"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <StoreOverview />
+        path="/dashboard/stores/:storeId"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <StoreOverview />
-      <Route
+          </RequireAuth>
-        path="/dashboard/stores/:storeId/profile"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <BusinessProfilePage />
+        path="/dashboard/stores/:storeId/profile"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <BusinessProfilePage />
-      <Route
+          </RequireAuth>
-        path="/dashboard/stores/:storeId/products"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <ProductList />
+        path="/dashboard/stores/:storeId/products"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <ProductList />
-      <Route
+          </RequireAuth>
-        path="/dashboard/stores/:storeId/promotions"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <StorePromotionsPage />
+        path="/dashboard/stores/:storeId/promotions"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <StorePromotionsPage />
-      
+          </RequireAuth>
-      {/* Loyalty - Main loyalty management page */}
+        }
-      <Route
+      />
-        path="/loyalty"
+      
-        element={
+      {/* Loyalty - Main loyalty management page */}
-          <RequireAuth>
+      <Route
-            <LoyaltyPage />
+        path="/loyalty"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <LoyaltyPage />
-      
+          </RequireAuth>
-      {/* Menu - Menu Engine page */}
+        }
-      <Route
+      />
-        path="/menu"
+      
-        element={
+      {/* Menu - Menu Engine page */}
-          <RequireAuth>
+      <Route
-            <MenuPage />
+        path="/menu"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <MenuPage />
-      
+          </RequireAuth>
-      {/* Promo - Promo Engine page */}
+        }
-      <Route
+      />
-        path="/promo"
+      
-        element={
+      {/* Promo - Promo Engine page */}
-          <RequireAuth>
+      <Route
-            <PromoPage />
+        path="/promo"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <PromoPage />
-      
+          </RequireAuth>
-      {/* Promo - Start new promotion (fast path) */}
+        }
-      <Route
+      />
-        path="/promotions/new"
+      
-        element={
+      {/* Promo - Start new promotion (fast path) */}
-          <RequireAuth>
+      <Route
-            <StartPromoPage />
+        path="/promotions/new"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <StartPromoPage />
-      
+          </RequireAuth>
-      {/* Legacy promo hub redirect */}
+        }
-      <Route
+      />
-        path="/app/creative-shell/promo"
+      
-        element={
+      {/* Legacy promo hub redirect */}
-          <RequireAuth>
+      <Route
-            <Navigate to="/promotions/new" replace />
+        path="/app/creative-shell/promo"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <Navigate to="/promotions/new" replace />
-      
+          </RequireAuth>
-      {/* Signage - Signage/Playlist Engine page */}
+        }
-      <Route
+      />
-        path="/signage"
+      
-        element={
+      {/* Signage - Signage/Playlist Engine page */}
-          <RequireAuth>
+      <Route
-            <SignagePage />
+        path="/signage"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <SignagePage />
-      
+          </RequireAuth>
-      {/* Playlist Editor - Cardbey Pro Editor */}
+        }
-      <Route
+      />
-        path="/signage/playlists/:playlistId"
+      
-        element={
+      {/* Playlist Editor - Cardbey Pro Editor */}
-          <RequireAuth>
+      <Route
-            <PlaylistEditorPage />
+        path="/signage/playlists/:playlistId"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <PlaylistEditorPage />
-
+          </RequireAuth>
-      {/* Suitcase — account knowledge vault (briefings, loyalty, artifacts) */}
+        }
-      <Route
+      />
-        path="/suitcase"
+
-        element={
+      {/* Suitcase — account knowledge vault (briefings, loyalty, artifacts) */}
-          <RequireAuth>
+      <Route
-            <SuitcaseVaultPage />
+        path="/suitcase"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <SuitcaseVaultPage />
-      <Route
+          </RequireAuth>
-        path="/suitcase/playlists/:playlistId"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <PlaylistEditorPage />
+        path="/suitcase/playlists/:playlistId"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <PlaylistEditorPage />
-      <Route
+          </RequireAuth>
-        path="/suitcase/cards"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <SuitcaseCardsPage />
+        path="/suitcase/cards"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <SuitcaseCardsPage />
-      <Route
+          </RequireAuth>
-        path="/suitcase/vault"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <SuitcaseVaultPage />
+        path="/suitcase/vault"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <SuitcaseVaultPage />
-
+          </RequireAuth>
-      {/* Devices - Device Engine page */}
+        }
-      <Route
+      />
-        path="/devices"
+
-        element={
+      {/* Devices - Device Engine page */}
-          <RequireAuth>
+      <Route
-            <DevicesPage />
+        path="/devices"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <DevicesPage />
-      
+          </RequireAuth>
-      {/* Devices - Screens (redirects to /devices for now, can be separate page later) */}
+        }
-      <Route
+      />
-        path="/devices/screens"
+      
-        element={
+      {/* Devices - Screens (redirects to /devices for now, can be separate page later) */}
-          <RequireAuth>
+      <Route
-            <DevicesPage />
+        path="/devices/screens"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <DevicesPage />
-      
+          </RequireAuth>
-      {/* Devices - Drones */}
+        }
-      <Route
+      />
-        path="/devices/drones"
+      
-        element={
+      {/* Devices - Drones */}
-          <RequireAuth>
+      <Route
-            <DronesPage />
+        path="/devices/drones"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <DronesPage />
-      
+          </RequireAuth>
-      {/* Devices - Robots */}
+        }
-      <Route
+      />
-        path="/devices/robots"
+      
-        element={
+      {/* Devices - Robots */}
-          <RequireAuth>
+      <Route
-            <RobotsPage />
+        path="/devices/robots"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <RobotsPage />
-      
+          </RequireAuth>
-      {/* Loyalty - Scan card to create loyalty program */}
+        }
-      <Route
+      />
-        path="/dashboard/loyalty/from-card"
+      
-        element={
+      {/* Loyalty - Scan card to create loyalty program */}
-          <RequireAuth>
+      <Route
-            <LoyaltyFromCardPage />
+        path="/dashboard/loyalty/from-card"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <LoyaltyFromCardPage />
-      
+          </RequireAuth>
-      {/* Legacy Dashboard (kept for backward compatibility) */}
+        }
-      <Route
+      />
-        path="/dashboard/legacy"
+      
-        element={
+      {/* Legacy Dashboard (kept for backward compatibility) */}
-          <RequireAuth>
+      <Route
-            <DashboardEnhanced />
+        path="/dashboard/legacy"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <DashboardEnhanced />
-      
+          </RequireAuth>
-      {/* Marketing Routes */}
+        }
-      <Route
+      />
-        path="/marketing/*"
+      
-        element={
+      {/* Marketing Routes */}
-          <RequireAuth>
+      <Route
-            <MarketingRoutes />
+        path="/marketing/*"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <MarketingRoutes />
-      
+          </RequireAuth>
-      {/* Legacy /app/performer → canonical Mission Console (/app?entry=performer); guest-safe */}
+        }
-      <Route path="/app/performer/*" element={<LegacyPerformerRedirect />} />
+      />
-      <Route path="/performer" element={<LegacyPerformerRedirect />} />
+      
-      
+      {/* Legacy /app/performer → canonical Mission Console (/app?entry=performer); guest-safe */}
-      {/* Creative Shell - Long-term container for Content Studio */}
+      <Route path="/app/performer/*" element={<LegacyPerformerRedirect />} />
-      <Route
+      <Route path="/performer" element={<LegacyPerformerRedirect />} />
-        path="/app/creative-shell/*"
+      
-        element={
+      {/* Creative Shell - Long-term container for Content Studio */}
-          <RequireAuth>
+      <Route
-            <CreativeShell />
+        path="/app/creative-shell/*"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <CreativeShell />
-      
+          </RequireAuth>
-      {/* Legacy Creative Engine Shell - Redirect to new shell */}
+        }
-      <Route
+      />
-        path="/app/creative-engine-shell"
+      
-        element={
+      {/* Legacy Creative Engine Shell - Redirect to new shell */}
-          <RequireAuth>
+      <Route
-            <Navigate to={contentStudioHome()} replace />
+        path="/app/creative-engine-shell"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <Navigate to={contentStudioHome()} replace />
-      
+          </RequireAuth>
-      {/* Show section — video advanced editor (audio mix timeline) */}
+        }
-      <Route
+      />
-        path="/app/show/edit"
+      
-        element={
+      {/* Show section — video advanced editor (audio mix timeline) */}
-          <RequireAuth>
+      <Route
-            <ShowVideoAdvancedEditorPage />
+        path="/app/show/edit"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <ShowVideoAdvancedEditorPage />
-
+          </RequireAuth>
-      {/* MI Greeting Cards */}
+        }
-      <Route
+      />
-        path="/app/mi-greeting-cards"
+
-        element={
+      {/* MI Greeting Cards */}
-          <RequireAuth>
+      <Route
-            <MiGreetingCardsPage />
+        path="/app/mi-greeting-cards"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <MiGreetingCardsPage />
-      
+          </RequireAuth>
-      {/* Catalog - Placeholder */}
+        }
-      <Route
+      />
-        path="/catalog"
+      
-        element={
+      {/* Catalog - Placeholder */}
-          <RequireAuth>
+      <Route
-            <CatalogPlaceholder />
+        path="/catalog"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <CatalogPlaceholder />
-      
+          </RequireAuth>
-      {/* Orders - Placeholder */}
+        }
-      <Route
+      />
-        path="/orders/seller"
+      
-        element={
+      {/* Orders - Placeholder */}
-          <RequireAuth>
+      <Route
-            <SellerOrdersPage />
+        path="/orders/seller"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <SellerOrdersPage />
-      <Route
+          </RequireAuth>
-        path="/orders/seller/:orderId"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <OrderDetailPage />
+        path="/orders/seller/:orderId"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <OrderDetailPage />
-      <Route
+          </RequireAuth>
-        path="/orders"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <OrdersPlaceholder />
+        path="/orders"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <OrdersPlaceholder />
-      
+          </RequireAuth>
-      {/* Screens - Redirect to Devices */}
+        }
-      <Route
+      />
-        path="/screens"
+      
-        element={
+      {/* Screens - Redirect to Devices */}
-          <RequireAuth>
+      <Route
-            <Navigate to="/devices" replace />
+        path="/screens"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <Navigate to="/devices" replace />
-      
+          </RequireAuth>
-      {/* Contents Studio */}
+        }
-      <Route
+      />
-        path="/contents"
+      
-        element={
+      {/* Contents Studio */}
-          <RequireAuth>
+      <Route
-            <ContentsStudio />
+        path="/contents"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <ContentsStudio />
-
+          </RequireAuth>
-      {/* Phase 9: Open slideshow artifact in Content Studio (client-only session handoff) */}
+        }
-      <Route
+      />
-        path="/content-studio/slideshows/:artifactId"
+
-        element={
+      {/* Phase 9: Open slideshow artifact in Content Studio (client-only session handoff) */}
-          <RequireAuth>
+      <Route
-            <SlideshowArtifactPreviewPage />
+        path="/content-studio/slideshows/:artifactId"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <SlideshowArtifactPreviewPage />
-      
+          </RequireAuth>
-      {/* Legacy Creative Engine - Redirect to Content Studio */}
+        }
-      <Route
+      />
-        path="/creative-engine"
+      
-        element={
+      {/* Legacy Creative Engine - Redirect to Content Studio */}
-          <RequireAuth>
+      <Route
-            <Navigate to={contentStudioHome()} replace />
+        path="/creative-engine"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <Navigate to={contentStudioHome()} replace />
-      
+          </RequireAuth>
-      {/* Legacy Contents Studio - Redirect to Content Studio */}
+        }
-      <Route
+      />
-        path="/app/contents-studio"
+      
-        element={
+      {/* Legacy Contents Studio - Redirect to Content Studio */}
-          <RequireAuth>
+      <Route
-            <Navigate to={contentStudioHome()} replace />
+        path="/app/contents-studio"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <Navigate to={contentStudioHome()} replace />
-      
+          </RequireAuth>
-      <Route
+        }
-        path="/admin/screens"
+      />
-        element={
+      
-          <RequireAuth>
+      <Route
-            <ScreensAdmin />
+        path="/admin/screens"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <ScreensAdmin />
-      
+          </RequireAuth>
-      {/* Account: edit in dashboard shell; public read-only at /account */}
+        }
-      <Route
+      />
-        path="/dashboard/account"
+      
-        element={
+      {/* Account: edit in dashboard shell; public read-only at /account */}
-          <RequireAuth>
+      <Route
-            <AccountProfilePage />
+        path="/dashboard/account"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <AccountProfilePage />
-      <Route path="/account" element={<AccountPublicProfilePage />} />
+          </RequireAuth>
-      
+        }
-      {/* Insights */}
+      />
-      <Route
+      <Route path="/account" element={<AccountPublicProfilePage />} />
-        path="/insights"
+      
-        element={
+      {/* Insights */}
-          <RequireAuth>
+      <Route
-            <Insights />
+        path="/insights"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <Insights />
-      
+          </RequireAuth>
-      {/* Orchestrator Tasks History */}
+        }
-      <Route
+      />
-        path="/orchestrator/tasks"
+      
-        element={
+      {/* Orchestrator Tasks History */}
-          <RequireAuth>
+      <Route
-            <OrchestratorTasks />
+        path="/orchestrator/tasks"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <OrchestratorTasks />
-      
+          </RequireAuth>
-      {/* Settings */}
+        }
-      <Route
+      />
-        path="/settings"
+      
-        element={
+      {/* Settings */}
-          <RequireAuth>
+      <Route
-            <Settings />
+        path="/settings"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <Settings />
-      <Route
+          </RequireAuth>
-        path="/settings/desktop"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <CardbeyDesktopSettings />
+        path="/settings/desktop"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <CardbeyDesktopSettings />
-      <Route
+          </RequireAuth>
-        path="/settings/integrations"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <IntegrationsPage />
+        path="/settings/integrations"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <IntegrationsPage />
-      <Route
+          </RequireAuth>
-        path="/settings/connect"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <ConnectAccounts />
+        path="/settings/connect"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <ConnectAccounts />
-      <Route
+          </RequireAuth>
-        path="/settings/signals"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <SignalsSettingsPage />
+        path="/settings/signals"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <SignalsSettingsPage />
-      
+          </RequireAuth>
-      {/* System Routes */}
+        }
-      <Route
+      />
-        path="/env"
+      
-        element={
+      {/* System Routes */}
-          <RequireAuth>
+      <Route
-            <Environment />
+        path="/env"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <Environment />
-      <Route
+          </RequireAuth>
-        path="/connect"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <ConnectAccounts />
+        path="/connect"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <ConnectAccounts />
-      <Route
+          </RequireAuth>
-        path="/api-keys"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <ApiKeys />
+        path="/api-keys"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <ApiKeys />
-      
+          </RequireAuth>
-      {/* Business Builder Routes */}
+        }
-      <Route
+      />
-        path="/onboarding/business"
+      
-        element={
+      {/* Business Builder Routes */}
-          <RequireAuth>
+      <Route
-            <RequireFeature feature="business_builder_v1">
+        path="/onboarding/business"
-              <BusinessDashboard />
+        element={
-            </RequireFeature>
+          <RequireAuth>
-          </RequireAuth>
+            <RequireFeature feature="business_builder_v1">
-        }
+              <BusinessDashboard />
-      />
+            </RequireFeature>
-      <Route
+          </RequireAuth>
-        path="/onboarding/business/*"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <RequireFeature feature="business_builder_v1">
+        path="/onboarding/business/*"
-              <BusinessDashboard />
+        element={
-            </RequireFeature>
+          <RequireAuth>
-          </RequireAuth>
+            <RequireFeature feature="business_builder_v1">
-        }
+              <BusinessDashboard />
-      />
+            </RequireFeature>
-      <Route
+          </RequireAuth>
-        path="/business/*"
+        }
-        element={
+      />
-          <RequireAuth>
+      <Route
-            <RequireFeature feature="business_builder_v1">
+        path="/business/*"
-              <BusinessDashboard />
+        element={
-            </RequireFeature>
+          <RequireAuth>
-          </RequireAuth>
+            <RequireFeature feature="business_builder_v1">
-        }
+              <BusinessDashboard />
-      />
+            </RequireFeature>
-      
+          </RequireAuth>
-      {/* Auth */}
+        }
-      <Route
+      />
-        path="/login"
+      
-        element={
+      {/* Auth */}
-          <RedirectIfAuthed>
+      <Route
-            <LoginPage />
+        path="/login"
-          </RedirectIfAuthed>
+        element={
-        }
+          <RedirectIfAuthed>
-      />
+            <LoginPage />
-      <Route path="/verify-email" element={<VerifyEmailRequiredPage />} />
+          </RedirectIfAuthed>
-      <Route path="/email-verified" element={<EmailVerifiedPage />} />
+        }
-      <Route
+      />
-        path="/signup"
+      <Route path="/verify-email" element={<VerifyEmailRequiredPage />} />
-        element={<SignupPage />}
+      <Route path="/email-verified" element={<EmailVerifiedPage />} />
-      />
+      <Route
-      <Route
+        path="/signup"
-        path="/signup/success"
+        element={<SignupPage />}
-        element={<SignupSuccessPage />}
+      />
-      />
+      <Route
-      <Route
+        path="/signup/success"
-        path="/verify"
+        element={<SignupSuccessPage />}
-        element={<VerifyEmailPage />}
+      />
-      />
+      <Route
-      <Route
+        path="/verify"
-        path="/forgot-password"
+        element={<VerifyEmailPage />}
-        element={<ForgotPasswordPage />}
+      />
-      />
+      <Route
-      <Route
+        path="/forgot-password"
-        path="/reset"
+        element={<ForgotPasswordPage />}
-        element={<ResetPasswordPage />}
+      />
-      />
+      <Route
-      
+        path="/reset"
-      {/* Test Marketing Layout - Temporary test route */}
+        element={<ResetPasswordPage />}
-      <Route
+      />
-        path="/test-marketing-layout"
+      
-        element={<TestMarketingLayout />}
+      {/* Test Marketing Layout - Temporary test route */}
-      />
+      <Route
-      
+        path="/test-marketing-layout"
-      {/* Store Reels Feed - full-screen vertical scroll of published stores */}
+        element={<TestMarketingLayout />}
-      <Route
+      />
-        path="/frontscreen/stores"
+      
-        element={<FrontscreenStoreReels />}
+      {/* Store Reels Feed - full-screen vertical scroll of published stores */}
-      />
+      <Route
-      {/* Explore — intent-based discovery (search, filters, structured results) */}
+        path="/frontscreen/stores"
-      <Route
+        element={<FrontscreenStoreReels />}
-        path="/frontscreen"
+      />
-        element={
+      {/* Explore — intent-based discovery (search, filters, structured results) */}
-          <React.Suspense fallback={<PublicFeedInstantShell />}>
+      <Route
-            <ExploreDiscoveryPage />
+        path="/frontscreen"
-          </React.Suspense>
+        element={
-        }
+          <React.Suspense fallback={<PublicFeedInstantShell />}>
-      />
+            <ExploreDiscoveryPage />
-      
+          </React.Suspense>
-      {/* Food Shorts Feed - YouTube Shorts-style vertical feed */}
+        }
-      <Route
+      />
-        path="/food"
+      
-        element={<FoodShortsFeed feedType="food" />}
+      {/* Food Shorts Feed - YouTube Shorts-style vertical feed */}
-      />
+      <Route
-      <Route
+        path="/food"
-        path="/food/shorts"
+        element={<FoodShortsFeed feedType="food" />}
-        element={<FoodShortsFeed feedType="food" />}
+      />
-      />
+      <Route
-      
+        path="/food/shorts"
-      {/* Device Pairing - Public Routes */}
+        element={<FoodShortsFeed feedType="food" />}
-      <Route path="/device/pair" element={<DevicePair />} />
+      />
-      <Route path="/device/player" element={<DevicePlayer />} />
+      
-      {/* QR entry — public, render-first; StorePromo.id in path */}
+      {/* Device Pairing - Public Routes */}
-      <Route path="/q/p/:promotionId" element={<PromotionQrEntryPage />} />
+      <Route path="/device/pair" element={<DevicePair />} />
-
+      <Route path="/device/player" element={<DevicePlayer />} />
-      {/* Campaign V2 — wizard (feature flag gated inside page) */}
+      {/* QR entry — public, render-first; StorePromo.id in path */}
-      <Route
+      <Route path="/q/p/:promotionId" element={<PromotionQrEntryPage />} />
-        path="/campaigns/new"
+
-        element={
+      {/* Campaign V2 — wizard (feature flag gated inside page) */}
-          <RequireAuth>
+      <Route
-            <CampaignNewPage />
+        path="/campaigns/new"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <CampaignNewPage />
-      {/* Campaign V2 — studio (Creative | Behavior | Deploy) */}
+          </RequireAuth>
-      <Route
+        }
-        path="/campaigns/:id/studio"
+      />
-        element={
+      {/* Campaign V2 — studio (Creative | Behavior | Deploy) */}
-          <RequireAuth>
+      <Route
-            <CampaignStudioPage />
+        path="/campaigns/:id/studio"
-          </RequireAuth>
+        element={
-        }
+          <RequireAuth>
-      />
+            <CampaignStudioPage />
-      {/* Legacy redirects - Already handled above */}
+          </RequireAuth>
-      <Route 
+        }
-        path="/campaigns/:id" 
+      />
-        element={
+      {/* Legacy redirects - Already handled above */}
-          <RequireAuth>
+      <Route 
-            <CampaignRedirect />
+        path="/campaigns/:id" 
-          </RequireAuth>
+        element={
-        } 
+          <RequireAuth>
-      />
+            <CampaignRedirect />
-      <Route path="/content" element={<Navigate to="/marketing/content" replace />} />
+          </RequireAuth>
-      <Route path="/leads" element={<Navigate to="/marketing/leads" replace />} />
+        } 
-      <Route path="/reports" element={<Navigate to="/marketing/reports" replace />} />
+      />
-      <Route path="/rewards" element={<Navigate to="/marketing/rewards" replace />} />
+      <Route path="/content" element={<Navigate to="/marketing/content" replace />} />
-      
+      <Route path="/leads" element={<Navigate to="/marketing/leads" replace />} />
-      {/* AI-first Console: /app (home), /app/missions, /app/missions/:id; guest-allowed so guests can create store missions and review drafts without sign-in; auth required at publish/claim */}
+      <Route path="/reports" element={<Navigate to="/marketing/reports" replace />} />
-      <Route
+      <Route path="/rewards" element={<Navigate to="/marketing/rewards" replace />} />
-        path="/app"
+      
-        element={
+      {/* AI-first Console: /app (home), /app/missions, /app/missions/:id; guest-allowed so guests can create store missions and review drafts without sign-in; auth required at publish/claim */}
-          <RequireAuthOrGuest>
+      <Route
-            <GuestPerformerEntryGuard>
+        path="/app"
-              <ConsoleShell />
+        element={
-            </GuestPerformerEntryGuard>
+          <RequireAuthOrGuest>
-          </RequireAuthOrGuest>
+            <GuestPerformerEntryGuard>
-        }
+              <ConsoleShell />
-      >
+            </GuestPerformerEntryGuard>
-        <Route index element={<ConsoleHomeWorkspace />} />
+          </RequireAuthOrGuest>
-        <Route path="missions" element={<MissionLogsListView />} />
+        }
-        <Route path="missions/:missionId" element={<MissionDetailView />} />
+      >
-        <Route path="missions/:missionId/artifacts/:artifactType" element={<MissionArtifactCreativeShell />} />
+        <Route index element={<ConsoleHomeWorkspace />} />
-        <Route path="missions/:missionId/artifacts/:artifactType/:artifactId" element={<MissionArtifactCreativeShell />} />
+        <Route path="missions" element={<MissionLogsListView />} />
-        <Route path="threads/:threadId" element={<ThreadChatView />} />
+        <Route path="missions/:missionId" element={<MissionDetailView />} />
-        <Route path="telemetry" element={<MissionConsoleTelemetryPage />} />
+        <Route path="missions/:missionId/artifacts/:artifactType" element={<MissionArtifactCreativeShell />} />
-        <Route path="console/control-tower" element={<ControlTowerPage />} />
+        <Route path="missions/:missionId/artifacts/:artifactType/:artifactId" element={<MissionArtifactCreativeShell />} />
-        {/* Dead-link fix: StoreReview and others navigate to /app/console — land on Control Tower */}
+        <Route path="threads/:threadId" element={<ThreadChatView />} />
-        <Route path="console" element={<Navigate to="/app/console/control-tower" replace />} />
+        <Route path="telemetry" element={<MissionConsoleTelemetryPage />} />
-      </Route>
+        <Route path="console/control-tower" element={<ControlTowerPage />
-
+        <Route path="development" element={<DevelopmentCenterPage />} />} />
-      {/* Console alias: /console mirrors /app performer console (same ConsoleShell + canonical sidebar) */}
+        {/* Dead-link fix: StoreReview and others navigate to /app/console — land on Control Tower */}
-      <Route
+        <Route path="console" element={<Navigate to="/app/console/control-tower" replace />} />
-        path="/console"
+      </Route>
-        element={
+
-          <RequireAuthOrGuest>
+      {/* Console alias: /console mirrors /app performer console (same ConsoleShell + canonical sidebar) */}
-            <GuestPerformerEntryGuard>
+      <Route
-              <ConsoleShell />
+        path="/console"
-            </GuestPerformerEntryGuard>
+        element={
-          </RequireAuthOrGuest>
+          <RequireAuthOrGuest>
-        }
+            <GuestPerformerEntryGuard>
-      >
+              <ConsoleShell />
-        <Route index element={<ConsoleHomeWorkspace />} />
+            </GuestPerformerEntryGuard>
-        <Route path="missions" element={<MissionLogsListView />} />
+          </RequireAuthOrGuest>
-        <Route path="missions/:missionId" element={<MissionDetailView />} />
+        }
-        <Route path="missions/:missionId/artifacts/:artifactType" element={<MissionArtifactCreativeShell />} />
+      >
-        <Route path="missions/:missionId/artifacts/:artifactType/:artifactId" element={<MissionArtifactCreativeShell />} />
+        <Route index element={<ConsoleHomeWorkspace />} />
-        <Route path="threads/:threadId" element={<ThreadChatView />} />
+        <Route path="missions" element={<MissionLogsListView />} />
-        <Route path="stores" element={<ConsoleSectionPlaceholder title="Stores" />} />
+        <Route path="missions/:missionId" element={<MissionDetailView />} />
-        <Route path="products" element={<ConsoleSectionPlaceholder title="Products" />} />
+        <Route path="missions/:missionId/artifacts/:artifactType" element={<MissionArtifactCreativeShell />} />
-        <Route path="analytics" element={<InsightsSectionPage />} />
+        <Route path="missions/:missionId/artifacts/:artifactType/:artifactId" element={<MissionArtifactCreativeShell />} />
-        <Route path="devices" element={<DevicesSectionPage />} />
+        <Route path="threads/:threadId" element={<ThreadChatView />} />
-        <Route path="creatives" element={<CreativesSectionPage />} />
+        <Route path="stores" element={<ConsoleSectionPlaceholder title="Stores" />} />
-        <Route path="integrations" element={<ConsoleSectionPlaceholder title="Integrations" />} />
+        <Route path="products" element={<ConsoleSectionPlaceholder title="Products" />} />
-        <Route path="telemetry" element={<MissionConsoleTelemetryPage />} />
+        <Route path="analytics" element={<InsightsSectionPage />} />
-        <Route path="control-tower" element={<ControlTowerPage />} />
+        <Route path="devices" element={<DevicesSectionPage />} />
-      </Route>
+        <Route path="creatives" element={<CreativesSectionPage />} />
-      
+        <Route path="integrations" element={<ConsoleSectionPlaceholder title="Integrations" />} />
-      {/* Layout Studio — full dashboard UI for General Layout Engine */}
+        <Route path="telemetry" element={<MissionConsoleTelemetryPage />} />
-      <Route path="/tools/layout-studio" element={<LayoutStudio />} />
+        <Route path="control-tower" element={<ControlTowerPage />} />
-
+      </Route>
-      {/* Global fallback - redirect to back office (but not if on preview) */}
+      
-      <Route path="*" element={<PreviewAwareFallback />} />
+      {/* Layout Studio — full dashboard UI for General Layout Engine */}
-    </Routes>
+      <Route path="/tools/layout-studio" element={<LayoutStudio />} />
-  );
+
-
+      {/* Global fallback - redirect to back office (but not if on preview) */}
-  return (
+      <Route path="*" element={<PreviewAwareFallback />} />
-    <QueryClientProvider client={queryClient}>
+    </Routes>
-      <GuestDraftClaimOnAuthEffect />
+  );
-      <ThemeProvider>
+
-        <ErrorBoundary>
+  return (
-          <RuntimeCapabilitiesProvider>
+    <QueryClientProvider client={queryClient}>
-          <ActiveMissionProvider>
+      <GuestDraftClaimOnAuthEffect />
-            <PublicStoreNotFoundProvider>
+      <ThemeProvider>
-            <DraftModeProvider>
+        <ErrorBoundary>
-              <SSEProvider
+          <RuntimeCapabilitiesProvider>
-                path="/events"
+          <ActiveMissionProvider>
-                authKey={
+            <PublicStoreNotFoundProvider>
-                  role === "admin" && shouldUseGlobalAdminDeviceSse(loc.pathname) ? "admin" : null
+            <DraftModeProvider>
-                }
+              <SSEProvider
-              >
+                path="/events"
-                <AppShell>
+                authKey={
-              <PwaShell />
+                  role === "admin" && shouldUseGlobalAdminDeviceSse(loc.pathname) ? "admin" : null
-              <PwaInstallSurface />
+                }
-              <GlobalShortcuts />
+              >
-              <GlobalPairAlerts />
+                <AppShell>
-            {/* Store review: mode=draft → no PageShell (StoreDraftReview renders its own top bar). mode=published → PageShell (PublicHeader only; StoreDraftReview hides its top bar to avoid duplicate logo/avatar). */}
+              <PwaShell />
-            {isLogin || isPerformer || isDevicePlayer || isFrontscreen || isBackOffice || isConsole || isBusiness || isBusinessHealth || isCreatorStudio || isPublicPage ? (
+              <PwaInstallSurface />
-              <>
+              <GlobalShortcuts />
-                {/* Skip PageTransition for fullscreen pages to avoid wrapper constraints */}
+              <GlobalPairAlerts />
-                {isFrontscreen ? (
+            {/* Store review: mode=draft → no PageShell (StoreDraftReview renders its own top bar). mode=published → PageShell (PublicHeader only; StoreDraftReview hides its top bar to avoid duplicate logo/avatar). */}
-                  routes
+            {isLogin || isPerformer || isDevicePlayer || isFrontscreen || isBackOffice || isConsole || isBusiness || isBusinessHealth || isCreatorStudio || isPublicPage ? (
-                ) : (
+              <>
-                  <PageTransition>
+                {/* Skip PageTransition for fullscreen pages to avoid wrapper constraints */}
-                    {routes}
+                {isFrontscreen ? (
-                  </PageTransition>
+                  routes
-                )}
+                ) : (
-                <Toasts />
+                  <PageTransition>
-              </>
+                    {routes}
-            ) : (
+                  </PageTransition>
-              <PageShell sidebar={sidebar} rightbar={rightbar}>
+                )}
-                <PageTransition>
+                <Toasts />
-                  {routes}
+              </>
-                </PageTransition>
+            ) : (
-                <Toasts />
+              <PageShell sidebar={sidebar} rightbar={rightbar}>
-              </PageShell>
+                <PageTransition>
-            )}
+                  {routes}
-              </AppShell>
+                </PageTransition>
-            </SSEProvider>
+                <Toasts />
-            </DraftModeProvider>
+              </PageShell>
-            <DevOverlaysGate />
+            )}
-            {/* Gate 1: Sign in / Sign up modal when guest clicks edit/publish on draft review */}
+              </AppShell>
-            <AuthRequiredModal />
+            </SSEProvider>
-            {/* System Watcher Chat Button - Now only in PublicFooter for public pages */}
+            </DraftModeProvider>
-            {/* WatcherChatButton is rendered in DashboardEnhanced underneath AI Insights for dashboard */}
+            <DevOverlaysGate />
-            {/* For public pages, it's in the footer via PublicFooter component */}
+            {/* Gate 1: Sign in / Sign up modal when guest clicks edit/publish on draft review */}
-            </PublicStoreNotFoundProvider>
+            <AuthRequiredModal />
-          </ActiveMissionProvider>
+            {/* System Watcher Chat Button - Now only in PublicFooter for public pages */}
-          </RuntimeCapabilitiesProvider>
+            {/* WatcherChatButton is rendered in DashboardEnhanced underneath AI Insights for dashboard */}
-        </ErrorBoundary>
+            {/* For public pages, it's in the footer via PublicFooter component */}
-      </ThemeProvider>
+            </PublicStoreNotFoundProvider>
-    </QueryClientProvider>
+          </ActiveMissionProvider>
-  );
+          </RuntimeCapabilitiesProvider>
-}
+        </ErrorBoundary>
+      </ThemeProvider>
+    </QueryClientProvider>
+  );
+}

--- a/apps/dashboard/cardbey-marketing-dashboard/src/components/development/developmentConsoleRouting.test.tsx
+++ b/apps/dashboard/cardbey-marketing-dashboard/src/components/development/developmentConsoleRouting.test.tsx
-
+import { describe, it, expect } from 'vitest';
+import fs from 'node:fs';
+import path from 'node:path';
+
+const appPath = path.resolve(__dirname, '../../App.jsx');
+
+describe('development console routing', () => {
+  it('classifies /app/development as console layout', () => {
+    const source = fs.readFileSync(appPath, 'utf-8');
+    expect(source).toMatch(/app\/development/);
+    expect(source).toContain('DevelopmentCenterPage');
+    expect(source).toContain('ConsoleShell');
+    expect(source).not.toMatch(/display:\\s*none.*sidebar/i);
+  });
+
+  it('DevelopmentCenterPage is content-only without nested shell', () => {
+    const pagePath = path.resolve(__dirname, '../../pages/development/DevelopmentCenterPage.tsx');
+    const pageSource = fs.readFileSync(pagePath, 'utf-8');
+    expect(pageSource).not.toMatch(/ConsoleShell|ConsoleSidebar|<Sidebar/);
+    expect(pageSource).toContain('DevelopmentTab');
+  });
+});
+