/**
 * Template Engine - Shared Package
 * 
 * Exports shared types and utilities for the Cardbey template system.
 * Used by both backend (Core) and frontend (Dashboard) for type safety.
 */

export * from './templateTypes';

